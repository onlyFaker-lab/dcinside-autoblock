# 크롬 웹스토어 등록 안내

제출할 때 필요한 것들과, 심사 양식에 그대로 붙여넣을 문구를 모아뒀습니다.

---

## 1. 미리 준비할 것

**개발자 계정 등록비 5달러** (최초 1회, 환불 없음)
https://chrome.google.com/webstore/devconsole 에서 구글 계정으로 결제합니다.

**개인정보 처리방침을 인터넷에 올리기**
`PRIVACY.md` 내용을 웹에 공개하고 그 주소를 제출 양식에 넣어야 합니다.
가장 간단한 방법은 GitHub Gist입니다. gist.github.com에 붙여넣고 공개로 저장한 뒤
주소를 복사하면 됩니다. GitHub Pages, 블로그, 노션 공개 페이지도 됩니다.

**스크린샷 1장 이상** (1280×800 또는 640×400 PNG)
확장 팝업을 띄운 화면을 캡처해서 크기를 맞추면 됩니다.
후보가 잡혀 있는 상태의 화면이 설명하기 좋습니다.

---

## 2. 업로드할 파일

`dcblock-store.zip` 을 그대로 올리면 됩니다.
설명 문서(README, PRIVACY, STORE)는 빼고 실행에 필요한 파일만 담았습니다.

---

## 3. 스토어 등록 정보 (복사용)

**이름**
```
디시 자동 갱신차단
```

**간단한 설명** (132자 이내)
```
차단이 만료된 악성 유저를 감지해 31일 재차단합니다. 디시인사이드 마이너 갤러리 매니저·부매니저용.
```

**자세한 설명**
```
디시인사이드 마이너 갤러리의 차단 기간은 최대 31일입니다. 그래서 악성 유저는 31일이
지나면 돌아와 같은 행동을 반복하고, 매니저는 그때마다 수동으로 다시 차단해야 합니다.

이 확장은 등록해둔 대상의 차단이 풀렸는지 하루 두 번 확인하고, 풀린 사람을 찾으면
알려줍니다. 확인 버튼을 누르면 31일로 다시 차단합니다.

■ 사용 조건
매니저(주딱) 또는 부매니저(파딱) 권한이 있는 마이너 갤러리에서만 동작합니다.
브라우저에 이미 로그인된 세션을 사용하므로 아이디와 비밀번호를 입력하거나
저장하지 않습니다.

■ 주요 기능
· 등록한 식별코드의 차단 상태를 정해진 시각에 자동 확인
· 차단이 풀린 대상을 목록으로 표시
· 확인 후 31일 재차단 (자동 실행 옵션은 기본 꺼짐)
· 차단이 실제로 걸렸는지 목록을 다시 읽어 검증
· 재차단 이력 기록

■ 안전장치
· 명단에 등록한 대상만 갱신합니다. 만료된 차단을 전부 자동 갱신하지 않습니다.
· 이미 차단 중인 대상은 제외해 중복 차단을 막습니다.
· 한 번에 처리할 최대 건수를 넘으면 실행하지 않고 멈춥니다.
· 조회는 하루 1~2회입니다. 짧은 주기로 서버에 요청하지 않습니다.

■ IP 차단을 지원하지 않는 이유
디시인사이드는 매니저에게도 IP 앞 두 자리까지만 보여줍니다. 그 상태로 차단하면
통신사 대역 전체를 막게 되어 무관한 이용자가 차단됩니다. 그래서 식별코드만 다룹니다.

■ 수집하는 정보
없습니다. 설정과 명단은 사용자 컴퓨터에만 저장되며 외부로 전송되지 않습니다.
```

**카테고리**: 소셜 및 커뮤니케이션
**언어**: 한국어

---

## 4. 권한 사용 사유 (심사 양식 복사용)

심사에서 권한마다 사유를 적어야 합니다. 아래를 그대로 넣으시면 됩니다.

**host_permissions (gall.dcinside.com)**
```
확장의 유일한 기능이 디시인사이드 마이너 갤러리의 차단 목록을 조회하고 재차단을
실행하는 것이므로, 해당 도메인에 대한 접근이 필요합니다. 다른 도메인에는 접근하지 않습니다.
```

**cookies**
```
디시인사이드는 관리 기능 요청에 ci_c 쿠키 값을 인증 토큰으로 요구합니다.
재차단 요청을 보낼 때 이 값을 함께 전달해야 하므로 해당 도메인의 쿠키 하나만 읽습니다.
읽은 값은 저장하거나 외부로 전송하지 않습니다.
```

**storage**
```
사용자가 등록한 차단 대상 명단, 확인 시각 등의 설정, 재차단 이력을 저장합니다.
모두 사용자의 브라우저 안에만 보관됩니다.
```

**alarms**
```
사용자가 지정한 시각(기본 하루 2회)에 차단 상태를 확인하기 위해 사용합니다.
```

**notifications**
```
차단이 만료된 대상을 발견했을 때 사용자에게 알리기 위해 사용합니다.
설정에서 끌 수 있습니다.
```

**원격 코드 사용**: 아니오 (모든 코드가 패키지에 포함되어 있습니다)

**데이터 사용 공개**: 아래 항목에 모두 "수집하지 않음"으로 체크
개인 식별 정보 / 건강 정보 / 금융 정보 / 인증 정보 / 개인 통신 내용 /
위치 / 웹 기록 / 사용자 활동 / 웹사이트 콘텐츠

세 개의 확인란(판매 안 함 / 용도 외 사용 안 함 / 신용도 판단 목적 사용 안 함)에 모두 체크.

---

## 5. 심사

보통 며칠 안에 결과가 나오지만, 권한이 많으면 더 걸릴 수 있습니다.
`cookies` 권한 때문에 추가 설명을 요구받을 가능성이 있으니, 위 사유를 명확히 적어두세요.

거절되면 사유가 메일로 옵니다. 대부분 설명을 보완하면 통과됩니다.

---

## 6. 등록하지 않고 배포하는 방법

웹스토어를 거치지 않아도 폴더째 나눠주면 됩니다.
받는 사람이 `chrome://extensions` → 개발자 모드 → 압축해제된 확장 프로그램 로드로
설치하면 동일하게 동작합니다.

다만 크롬이 주기적으로 "개발자 모드 확장을 사용 중지하시겠습니까" 하고 물어보는 점,
그리고 업데이트를 수동으로 배포해야 하는 점이 불편합니다.
사용자가 여럿이면 웹스토어 등록이 낫습니다.
