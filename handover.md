# 디시 자동 갱신차단 — 인수인계서

> 이 문서 하나만 읽고 유지보수를 이어받을 수 있도록 작성했습니다.
> 2026-09-12 기준. 크롬 확장 **v1.7.8** 이 최신입니다.
> 저장소: `https://github.com/onlyFaker-lab/dcinside-autoblock`
>
> **⚠ 이 문서는 v1.7.3 이후 네 버전 동안 갱신되지 않았습니다.** 2026-09-12에
> 1.7.4~1.7.7 내용을 넣으며 정리했습니다. 버전을 올릴 때 이 문서도 같이
> 고치세요. 안 고치면 다음 사람이 옛 기준값을 믿고 엉뚱한 곳을 봅니다
> (5-6절에 실제로 그랬던 사례가 있습니다).

---

## 0. 지금 당장 알아야 할 것

**미해결 5건입니다. 5절에 자세히 있습니다.** (재차단 실전 검증은 2026-09-09에 끝났습니다)

1. ~~재차단이 실제로 걸리는지~~ → **2026-09-09 성공했습니다.** 주딱 갤에서 두 명이 31일로 재차단됐고 관리 화면에서 눈으로 확인했습니다. 5-1절. **다음 확인은 10월 10일 자연 만료입니다.**
2. **파딱 갤 9월 9일 31일 만료** — 실제 31일 차단이 만료되는 첫 사례입니다. **파딱이 테스트할 수 없는 상황이 됐습니다.** 다만 디시가 해제됨 기록을 30일 보관하므로 **10월 9일 전에 복귀하면 늦게라도 그대로 검증됩니다.** 조건은 그 사이 아무도 그 사람들을 다시 차단하지 않는 것입니다(최신 행이 바뀌면 판정 대상이 달라집니다). 5-1절. *(2026-09-12 현재 결과 미확인)*
3. **IP 차단이 실제로 났습니다 (2026-09-08).** v1.6.5에서 대응했지만 새 간격이 충분한지는 실전에서 확인 못 했습니다. 5-5절.
4. **디시가 차단한 갤로그 화면** — 못 구해서 그 계정은 걸러내지 못합니다. 5-3절.
5. **v1.7.4~v1.7.7은 실제 디시로 확인한 것이 하나도 없습니다.** 검사만 통과한 상태입니다. 특히 `실행 전에 후보를 다시 확인`은 완장이 둘 이상이어야 뜻이 있는 기능이라 주딱 갤 혼자서는 검증이 안 됩니다. 5-9절.

**디시는 IP 단위로 막습니다.** 완장 계정이 아니라 회선이 막혀서, 그 집에서는 디시 자체가 안 열립니다. 요청 간격을 줄이자는 생각이 들면 5-5절을 먼저 읽으세요.

**이 프로젝트의 버그는 거의 전부 "조용한 실패"였습니다.** 에러 없이, 완장은 잘 돌아간다고 믿는데 실제로는 아무도 안 막히는 상태. 4절 원칙을 먼저 읽으세요.

### 2026-09-12 저녁 — 파딱 실사용 기록에서 드러난 것 (가장 최근)

v1.7.8. **확장이 몇 달을 돌아도 한 명도 진행시키지 못하는 상태였습니다.** 에러도 없이.

파딱이 보내준 작업 기록입니다.

```
[10:46:30] 명단 4577명 중 300명을 조회합니다
[10:47:23]   [25/300] resign7521: 2건 조회 — 이상 없음
[10:47:56]   [50/300] concerto8599: 2건 조회 — 이상 없음
[11:25:53] [안내] 끝나지 않은 채 남아 있던 작업 표시를 풀었습니다.
[14:00:59] 명단 4577명 중 300명을 조회합니다        ← 똑같이 다시 시작
[14:01:31]   [25/300] resign7521: 2건 조회 — 이상 없음   ← 똑같은 사람
[14:40:53] [안내] 끝나지 않은 채 남아 있던 작업 표시를 풀었습니다.
[18:06:30] 명단 4577명 중 300명을 조회합니다        ← 또
```

**세 번 다 같은 사람부터 시작해서 50명쯤에서 죽었습니다.** 원인 세 가지가 겹쳐 있었습니다.

**① MV3 서비스워커는 30초 동안 chrome API 호출이 없으면 종료됩니다.**
`fetch` 와 `setTimeout` 은 그 타이머를 되살리지 못합니다. 이건 MV3 의 성질이지 버그가 아닙니다. 그런데 조회 루프에서 chrome API 를 부르는 곳은 `touchBusy()` 하나뿐이었고 **25명마다** 였습니다. 한 명당 1.3초니 33초 간격입니다. **30초 문턱을 넘깁니다.** 25명은 아슬아슬하게 넘기고 50명에서 죽었습니다.

이제 `setStatus(text, true)` 가 20초짜리 keepAlive 를 겁니다. 긴 작업은 전부 이 한 곳을 지나므로 경로마다 따로 챙길 필요가 없습니다. 경로마다 붙이면 새 경로를 만들 때 빠뜨리고, **빠뜨려도 조용히 죽기만 합니다.**

**② 죽으면 그때까지 한 일이 통째로 사라졌습니다.**
결과를 마지막에 한꺼번에 저장하는 구조였습니다. 50명을 조회하고 죽으면 50명분이 전부 없어지고, 그 사람들은 `nextCheckAt` 이 그대로라 다음 차례에 똑같이 뽑힙니다. **이게 없었으면 ①만 고쳐도 됐을 텐데, 둘이 겹쳐서 영구 정지가 됐습니다.** 이제 25명마다 중간 저장합니다.

후보는 중간 저장하지 않아도 됩니다. `candidate` 판정은 `nextCheckAt` 이 '지금'이라 다음 차례에 어차피 다시 뽑힙니다. 반면 '이상 없음'으로 끝난 사람은 만료까지 안 봐도 되므로 그 판정을 잃으면 안 됩니다.

**③ 4577명 전원이 매번 조회 대상이었습니다.**
3-3절의 설계는 "만료 예정 시각을 기억해두고 그때까지 조회하지 않는다" 입니다. 그런데 **명단 채우기로 담는 경로만 그 설계 밖에 있었습니다.** 담을 때 `nextCheckAt` 을 안 넣어서 0 이 됐고, 0 은 '한 번도 못 봤다'는 뜻이라 만료가 한 달 남은 사람도 대상이 됐습니다.

차단 목록에는 처리 시각과 기간이 **이미 다 있습니다.** 디시에 더 묻지 않고 담는 순간 계산할 수 있었습니다. 이제 그렇게 합니다.

**옛 명단은 자동으로 안 낫습니다.** 이미 `nextCheckAt: 0` 으로 담긴 4577명은 그대로입니다. 채우기를 한 번 더 돌리고 담기를 누르면 **이미 명단에 있는 사람의 빈 만료 시각을 채워 줍니다.** 이게 옛 명단을 고치는 유일한 길입니다 (v1.7.5의 갤로그 기록 되메우기와 같은 방식).

**④ 곁가지 — 로그아웃인데 계획부터 안내했습니다.**
`9분쯤 걸립니다` `8일쯤 걸려 한 바퀴를 돕니다` 를 다 읽은 다음 줄에 `[오류] 로그인이 풀린 것 같습니다` 가 왔습니다. 완장은 9분짜리 작업이 시작된 줄 알고 기다립니다. 이제 첫 조회가 성공한 뒤에 안내합니다.

**⑤ 곁가지 — 굳은 잠금 30분.**
10:47 에 죽은 작업의 잠금이 11:25 까지 살아 있었습니다. 38분입니다. "300명이 9분대니 30분이면 여유"라는 계산이었는데, 그 계산은 **작업이 끝까지 돈다는 전제**였습니다. keepAlive 가 20초마다 `busySince` 를 갱신하므로 이제 3분이면 충분합니다.

**완장이 물은 것 — "로그아웃 상태에서도 후보가 떠야 하지 않나".** 맞는 말이고, ③을 고치면 저절로 됩니다. 만료 예정 시각이 명단에 있으면 디시에 묻지 않고도 "몇 명이 만료됐다"를 셀 수 있습니다. 지금까지 그 값이 전원 0 이라 셀 수가 없었던 것입니다. 다만 **후보 목록 자체(닉네임·사유)를 띄우려면 조회가 필요합니다.** 해제됐는지는 차단 목록을 봐야 알 수 있기 때문입니다. 로그아웃 상태에서 할 수 있는 건 "N명이 만료 예정 시각을 지났습니다" 까지입니다.

### 2026-09-12 낮에 있었던 일

v1.7.7. **v1.7.6의 새 기능이 켜는 순간 터지는 상태로 나가 있었습니다.** 검사 257개를 전부 통과하면서요.

`실행 전에 후보를 다시 확인`(1.7.6)이 `analyzeCode(rows, c.code)` 로 불렀는데, 그 함수의 `candidate` 갈래가 마지막 줄에서 `entry.reason` 을 읽습니다. **`entry` 를 안 넘겨서 TypeError 가 났습니다.**

터지는 자리가 하필 정상 경로입니다.

| 후보 상태 | 결과 |
|---|---|
| **아직 풀려 있음** (= 재차단해야 함) | **터짐.** `Cannot read properties of undefined (reading 'reason')` |
| 그 사이 다른 완장이 차단함 | 무사히 지나감 |
| 목록에 흔적 없음 | 무사히 지나감 |

즉 **아무것도 안 해도 되는 날에만 정상으로 보입니다.** 완장이 보는 것은 `[오류] Cannot read properties of undefined` 한 줄이고, 한 명도 차단되지 않습니다.

같이 고친 것들입니다.

| 무엇 | 왜 |
|---|---|
| 한도 검사를 재확인 **앞으로** | 순서가 반대라, 후보 300명에 한도 100이면 300번을 다 조회한 뒤에 `한도 초과로 중단`이 떴습니다. 한 명도 못 막으면서 사람당 한 요청이 300번. 5-5절 사고와 모양이 같습니다 |
| 재확인 도는 동안 **잠금** | 안 잠가서, 그 사이 알람이 정기 확인을 띄우면 두 경로가 겹쳐 돕니다. 요청이 두 배면 1.2초 간격이 무의미해집니다 |
| 재확인 중 **표를 못 읽으면 중단** | 상태 칸을 못 읽으면 해제된 사람이 전부 '차단 중'으로 보입니다. 그 판정으로 빼면 조용히 재차단을 건너뜁니다 |
| 판정이 바뀐 사람을 **따로 알림** | `active`(다른 완장이 차단)와 `manual`/`none`(손으로 풀림·목록에서 사라짐)은 원인이 다릅니다. 뭉개면 왜 안 막혔는지 알 수 없습니다 |

**교훈이 하나 늘었습니다. 소스에 글자가 있는지 보는 검사는 동작 검사가 아닙니다.** 1.7.6의 검사는 `background.js` 를 문자열로 읽어 `if (settings.recheckBeforeApply)` 가 있는지, 중단 문구가 있는지를 정규식으로 봤습니다. 전부 통과했고, 코드는 켜자마자 죽었습니다. **그 글자가 실제로 도는지는 돌려봐야 압니다.** 원칙 8번으로 넣었습니다.

그래서 `test-bg.mjs` 를 만들었습니다. 가짜 디시를 세워 두고 `background.js` 원본을 그대로 돌립니다. **가짜 디시는 POST 를 받으면 실제로 상태를 바꿉니다** — 응답만 흉내내면 "차단되었습니다라고 답하고 아무 일도 안 한다"는 디시의 성질(원칙 1번)을 검사가 못 보게 됩니다. 요청이 몇 번 나갔는지도 셉니다.

새 검사 44개는 전부 **일부러 되돌려서 실패하는 것을 확인한 뒤에** 넣었습니다.

| 되돌린 것 | 잡히는 검사 |
|---|---|
| `analyzeCode` 에서 entry 빼기 (= 1.7.6) | 15개 |
| 한도 검사를 재확인 뒤로 | 1개 (`재확인 조회 0회: 기대 0 / 실제 3`) |
| 표 못 읽음 검사 빼기 | 4개 |
| 재확인 중 잠금 빼기 | 1개 |
| 판정 바뀐 사람 뭉개기 | 2개 |

### 2026-09-10에 있었던 일

v1.7.4 ~ v1.7.6. 전부 파딱 피드백입니다.

| 무엇 | 내용 |
|---|---|
| **로그인 풀림 때 0명** | 대상을 고를 때는 `nextCheckAt === 0`(명단 채우기로 막 담은 사람)을 '봐야 할 사람'으로 치는데 **세는 쪽만 0을 걸러냈습니다.** 파딱 갤 4575명이 전원 0이라 한 명도 안 세어졌습니다. 이제 고른 `due` 를 그대로 씁니다 |
| **예상 시간이 늘 모자람** | 요청 간격만 세고 조회 시간을 빼먹었습니다. 300명에 `6분쯤`이라 해놓고 8~9분. 완장이 멈춘 줄 알고 기다렸습니다. `FETCH_SECS = 0.5` 를 더합니다 |
| **며칠에 걸쳐 한 바퀴** | 4575명이면 300명씩 16번, 하루 2번이면 8일입니다. 설명이 없어서 고장으로 보였습니다 |
| **명단 파일에 갤로그 기록 동봉** | 완장이 여럿이면 같은 사람을 각자 조회합니다. 한 명이 돌고 파일로 넘기면 나머지는 **조회 0번**으로 기준점을 얻습니다 |
| **실행 전 재확인 토글** | 다른 완장이 이미 차단한 사람을 빼고 보냅니다. 기본 꺼짐 |

**갤로그 기록 합치기 규칙이 이 기능의 전부입니다.** 최신으로 덮으면 공유할수록 기준점이 뒤로 밀려서 아무도 판정을 못 받게 됩니다. 우리가 원하는 건 반대 — 창을 넓히는 것입니다.

```
숫자가 같다   → 두 관측 사이에 아무 일도 없었다.
                gallogSince 는 이른 쪽, gallogCountedAt 은 늦은 쪽.
숫자가 다르다 → 사이에 글을 썼다. 오래된 관측은 무효. 늦은 쪽을 통째로.
```

`dc.js` 의 `mergeGallog()` 한 곳에만 있고 `popup.js` 가 가져다 씁니다. **v1.7.5부터 `popup.js` 가 `dc.js` 를 import 합니다.** 옮겨 적으면 규칙이 두 벌이 됩니다.

### 2026-09-08에 있었던 일 (이어받는 사람은 이것부터)

이날 하루에 v1.6.5까지 왔고, **실제 브라우저로 디시를 열어 확인**하면서 버그 6개를 찾았습니다. 전부 코드만 읽어서는 안 나왔을 것들입니다.

| 찾은 것 | 어떻게 드러났나 |
|---|---|
| **갤로그 천 단위 쉼표** | 실제 갤로그를 열어 숫자를 눈으로 봄 |
| 하루 한도 때 `0건 모두 차단 확인됨` | 실제로 한도에 걸려봄 |
| 하루 한도 때 `완료: 0/0건 확인` | 위와 같음 |
| 예상 시간 `1분` (실제 3초) | 실제로 돌려봄 |
| 상태 칸 파싱 실패가 경보에 안 잡힘 | 코드 정독 |
| 안 본 사람의 판정이 사라짐 | 코드 정독 |

**교훈이 하나 있습니다.** 파딱의 "258명 실패"를 처음에는 IP 차단 탓으로만 봤습니다. 시간대도 맞았고 그럴듯했습니다. 그대로 믿었으면 진짜 원인인 쉼표를 못 찾았을 것입니다. **실패의 원인도 추측하지 말고 실물을 보세요.** 원칙 7번이 화면 구조뿐 아니라 원인 분석에도 적용됩니다.

### 2026-09-09 오후 — 브라우저로 다시 확인한 것

v1.7.2. **다른 크롬(로그인 안 된 창)이 걸린 덕에 오히려 버그가 나왔습니다.**

| 한 것 | 결과 |
|---|---|
| 로그아웃 상태 관리 화면 응답 | **87바이트 스크립트 한 줄.** 한글 없음. 2-9절 |
| 그때 확장이 내는 메시지 | **"매니저 권한을 확인하세요" — 틀린 진단.** v1.7.2에서 고침 |
| `ci_c` 쿠키 | **로그아웃해도 남는다.** 쿠키로 로그인 여부를 판단하면 안 됨 |
| 공지 필터 (큰 갤) | **확인 완료.** 프로그래밍 갤 52행, 공지 1·광고 2 제외, 49글 |
| 차단 창 실물 대조 | **전부 일치.** 기간 6개, 사유 7+직접입력(`0`), 20자, 500자 |
| v1.7.3 화면 기능 | **주딱이 직접 눌러 확인 완료** — 직접 입력 사유(한 명·여러 명), 수정, 중지만 보기, 사유 검색 |

로그인이 풀린 창을 만난 건 사고였지만, 그 덕에 **평소에는 재현하기 어려운 경로**를 실물로 봤습니다. 재현하려면 로그아웃을 해야 하는데 그러면 다른 검증을 못 하니 미뤄지던 자리였습니다.

### 2026-09-09에 있었던 일

v1.6.6 ~ v1.6.9. **주딱 갤에서 한도와 무관한 기능을 전부 실측했고, 화면을 다시 짰습니다.**

| 한 것 | 결과 |
|---|---|
| `runCheck`의 400ms 고정 간격 발견 | v1.6.6. 갤로그만 고치고 여기를 빠뜨렸었다 |
| 주딱 갤 전체 기능 실측 | 5-7절. 12시간 규칙·탈퇴 판정·활동 점검 등 확인 |
| 화면 재구성 (탭 6 → 4) | v1.6.7 |
| DCRefresher Reborn 참고 UI | v1.6.8 → **v1.6.9에서 실물 캡처 보고 고침** |
| **재차단 실전 검증** (0시 이후) | **성공.** 5-1절 |
| **직접 입력 사유 문제 발견·해결** | v1.7.0. 아래 참고 |
| 파딱 피드백 6건 반영 | v1.7.1 |

**직접 입력 사유가 이 프로젝트에서 가장 컸던 조용한 실패였습니다.** 디시 차단 창은 라디오 7개 외에 `직접 입력` 칸이 있고, 완장들은 주로 이쪽을 씁니다. 우리 코드는 7개만 알아서 ① 명단 파일을 불러오면 전부 `음란성`이 되고 ② 재차단할 때는 `알 수 없는 사유`로 던졌습니다. **파딱 갤 명단 4533명은 전원이 직접 입력 사유라 한 명도 재차단되지 않았을 것입니다.** 주딱 갤 테스트가 전부 `음란성`이라 여태 안 걸렸습니다.

실제 요청을 받아 보니 직접 입력은 `avoid_reason=0` + `avoid_reason_txt=원문`이었습니다. **'직접 입력'이 여덟 번째 라디오라 8일 거라고 짐작했는데 0이었습니다.** 추측으로 넣었으면 4533명이 엉뚱한 사유로 차단됐을 것입니다. 원칙 7번이 또 맞았습니다.

**2026-09-09 오후 차단 창을 열어 독립 확인했습니다.** 4533명이 걸린 문제라 두 번 확인할 값어치가 있었습니다.

| 항목 | 실물 | 확장 |
|---|---|---|
| `avoid_hour` | `1 / 6 / 24 / 168 / 336 / 744` | `HOURS_BY_LABEL` 일치 |
| `avoid_reason` 라디오 | `1~7` + **`0`(직접 입력)** | `REASON_VALUES` + `CUSTOM_REASON="0"` 일치 |
| 직접 입력 칸 | `maxlength=20`, `한글 20자 이내` | `REASON_TXT_MAX=20` 일치 |
| `user_codes` | `maxlength=500` | 500자 분할 일치 |

**⚠ DOM에서는 직접 입력 칸의 `name`이 `avoid_reason_txt`가 아니라 `avoid_reason`입니다.** 라디오와 이름이 같습니다. 그래서 마크업만 보면 우리 코드가 틀린 것처럼 보입니다. 실제로는 `managements.js`가 `avoid_reason == 0` 분기에서 `avoid_reason_txt`를 따로 만들어 보냅니다(그 파일에 여섯 군데 나옵니다). **폼 마크업이 아니라 전송 스크립트를 봐야 합니다.**

**교훈이 또 하나 늘었습니다.** v1.6.8에서 "디시 확장들은 어두운 남색을 쓴다"고 짐작해 색을 바꿨는데, 실제 캡처를 받아 보니 **중성 짙은 회색**이었습니다. 원칙 7번은 디시 마크업뿐 아니라 **참고하려는 다른 프로그램의 화면에도** 적용됩니다. 캡처 한 장이면 끝날 일을 추측으로 메우지 마세요.

그리고 **설명 문구도 코드입니다.** v1.6.6에서 조회 간격을 1.2초로 올렸는데 설정 화면은 `한 명당 0.4초`라고 말하고 있었습니다. 완장은 그 문구를 보고 기다릴 시간을 가늠합니다. 지금은 화면 문구와 실제 상수가 어긋나면 검사가 실패합니다.

## 1. 무엇을 만들었나

디시인사이드 마이너 갤러리의 차단 기간 최대치는 **31일**입니다. 악성 유저가 31일 뒤 돌아와 같은 짓을 반복하고, 완장은 매번 수동으로 다시 차단해야 합니다.

이 프로그램은 **등록해둔 대상의 차단이 풀렸는지 하루 두 번 확인하고, 풀렸으면 31일로 재차단**합니다. 자동으로 차단하지 않고 후보만 보여준 뒤 사용자가 버튼을 눌러야 실행됩니다.

파이썬 GUI 버전도 있었으나 저장소에 없고 유지보수 중단됐습니다. **크롬 확장만 다룹니다.**

### 등장인물

- **의뢰인(주딱)** — `90-00년대 추억 갤러리`(`90_00_memory`) 매니저. 개발 경험은 있지만 깃허브·크롬 확장은 처음입니다. 터미널 명령이나 클릭 순서를 구체적으로 짚어주세요.
- **파딱** — `티원 응원 갤러리`(`t1win`) 부매니저. 실사용자이자 피드백 제공자. 갤러리 게시판 댓글로 소통합니다.

`90_00_memory`는 이력 49건짜리 작은 갤러리라 테스트용으로 좋습니다. `t1win`은 하루 수백 글이 올라오는 대형 갤러리입니다.

---

## 2. 디시 관리 화면에 대해 알아낸 사실

**이 절이 가장 중요합니다.** 전부 실제 화면과 통신을 확인해서 얻은 것이고, 대부분 문서에 없는 내용입니다.

### 2-1. 차단 목록 페이지

```
https://gall.dcinside.com/mgallery/management/block?id=<갤러리ID>
```

| 파라미터 | 값 |
|---|---|
| `s` | 상태 필터 (A=전체 B=차단중 R=해제됨) |
| `s_type` | `search_nickname` / `search_user_id` / `search_user_ip` / `search_subject` / `search_comment` / `search_reason` |
| `s_keyword` | 검색어 |
| `t` | `u`=이용자 차단, `i`=이미지 차단 |
| **`p`** | **페이지 번호** |

**상태 필터(`s=`)는 믿으면 안 됩니다.** `s=B`(차단 중)로 걸렀는데 해제됨 행이 섞여 나옵니다. 코드는 필터를 쓰지 않고 상태를 직접 읽습니다.

**한 페이지 30행입니다.**

**⚠ 페이지 파라미터가 `page`가 아니라 `p`입니다.** 실제 페이저가 내보내는 주소:
```
/mgallery/management/block?id=90_00_memory&s=&t=u&p=2
```

**⚠ `bottom_paging_box` 클래스가 페이지에 두 개 있습니다.** 하나는 화면 위쪽 헤더의 즐겨찾기 레이어 안(`<em>1</em>` 뿐, 링크 없음), 진짜 목록 페이저는 아래쪽입니다. `html.match()`는 첫 번째만 돌려주므로 위쪽을 잡으면 링크가 하나도 안 나옵니다. **v1.5.3까지 명단 채우기가 1페이지만 불러오던 원인이 이것입니다.**

지금은 페이저 링크를 아예 보지 않습니다. `p` 번호를 직접 올리고, 행이 없거나 앞 페이지와 같은 내용이면 멈춥니다.

### 2-2. 차단 목록 테이블 구조

```
table.minor_block_list > tbody > tr
  td.gall_chk             체크박스
  td.blocknum[data-num]   내부 차단 ID (행마다 고유)
  td.blocknik             닉네임 + (식별코드 또는 IP)
  td.blockcontent         게시글/댓글 링크 또는 '직접 차단'
  td.blockreason          사유
  td.blocktime            차단 기간 ('31일')
  td.blockday             span.block_date(날짜)
                          + 숨겨진 p.block_time('처리 시간 : 15:06:30')
                          + 숨겨진 p.block_conduct('처리자 : ...')
  td.blockstate.txtbtn    '해제됨'  또는  '차단 중' + 해제 버튼
```

실제 마크업의 함정들입니다.

- **`td.blockstate`의 진짜 클래스는 `blockstate txtbtn`입니다.** 완전일치로 잡으면 못 잡고, 못 잡으면 `released`가 전부 false가 되어 **후보가 한 명도 안 잡히는데 경고도 안 뜹니다.** 그래서 상태 칸을 읽지 못한 행은 `stateUnknown`으로 표시해 경고에 셉니다.
- 차단 중인 행의 상태 칸 텍스트는 해제 버튼까지 합쳐 `차단 중 해제`가 됩니다. 판정은 `해제됨` 포함 여부라 오판하지 않습니다.
  (버튼: `set_avoid(this, 'R', '<차단번호>', 0)`)
- **처리 시각이 숨겨진 툴팁 안에 있습니다.** 화면에는 날짜만 보이지만 DOM에는 초 단위 시각이 있습니다. 이 덕분에 만료 시각을 계산할 수 있고, 그게 이 프로그램의 핵심 장치 두 개(수동 해제 구분, 대량 명단 스케줄링)를 가능하게 합니다.
- **`td.blocknik` 안에 jQuery 템플릿 `<script>`가 들어 있습니다.** "중복 차단으로 기존 차단은 해제되었습니다" 같은 문구가 있어서 제거하지 않으면 파싱이 오염됩니다.
- **닉과 코드가 빈 `<p></p>` 껍데기에 싸여 있습니다.**
  `<p></p><p>ㅇㅇ</p><p></p>` 다음 줄에 `<p></p><p>(chip3298)</p><p></p>` 식입니다.
- **닉네임 자체에 괄호가 들어갈 수 있습니다.** `송재원(새우젓눈깔) (119.65.*.*)` 처럼요. **마지막** 괄호 그룹을 식별자로 봐야 합니다.
- **검색 결과에서는 검색어가 태그로 감싸져 강조됩니다.** `(lea<span>f451</span>7)`처럼 단어 중간이 쪼개질 수 있어서, 태그를 **공백 없이** 제거해야 합니다. 공백으로 치환하면 코드가 `lea f451 7`로 깨집니다.
- 체크박스 `input[name="chk_avoid_user[]"]`의 **id가 모든 행에서 `list_chk`로 중복**입니다.

### 2-3. 갤러리 글 목록 (활동 점검용, v1.6.0)

```
https://gall.dcinside.com/mgallery/board/lists/?id=<갤러리ID>&list_num=100&page=2
```

**⚠ 여기는 페이지 파라미터가 `page`입니다.** 차단 목록(`p`)과 다릅니다. 두 곳이 다르니 헷갈리지 마세요.

`list_num=100`으로 한 페이지 100개까지 받을 수 있습니다. 기본 50이라 안 쓰면 요청이 두 배가 됩니다.

**글쓴이 칸에 식별 코드가 그대로 들어 있습니다.** 이게 활동 점검의 핵심입니다.

```html
<td class="gall_writer ub-writer" data-nick="ㅇㅇ" data-uid="nanny1568" data-ip="">    ← 고닉/반고닉
<td class="gall_writer ub-writer" data-nick="응붕이" data-uid="" data-ip="220.85">     ← 유동
```

`data-uid`가 차단 목록의 식별코드와 같은 값입니다. **덕분에 목록을 한 번 훑으면 명단 전원의 활동 여부를 동시에 알 수 있습니다.** 사람마다 조회하는 방식이 아니라 명단이 커져도 요청이 늘지 않습니다.

함정 두 개입니다.

- **공지는 매 페이지에 붙박이로 딸려 나옵니다.** 그냥 읽으면 작년 공지 날짜 때문에 1페이지에서 바로 멈추고, 공지 쓴 완장은 영원히 "활동 중"이 됩니다. `data-type="icon_notice"`로 걸러야 합니다.
- **설문/광고 줄**은 `data-no`가 없고 글쓴이 칸 형태가 다릅니다(`user_name="운영자"`). 날짜 형식도 `26/08/27`로 다릅니다.

날짜는 `<td class="gall_date" title="2026-09-07 17:05:43">`의 `title`에 초 단위로 있습니다. 본문 텍스트는 당일이면 시각만 나옵니다.

**댓글은 목록에 안 나옵니다.** 이게 활동 점검의 근본 한계입니다. 댓글로만 활동하는 사람은 "글 없음"으로 잡힙니다.

> 갤 내부 검색에 `댓글` 옵션이 있지만 **댓글 내용을 키워드로 찾는 것**이고, 결과는 글 목록이며 표시되는 글쓴이는 글 작성자입니다. 댓글 작성자로 찾는 방법은 없어 보입니다. (미확인 — 직접 검증하면 좋습니다)

### 2-4. 갤로그 (탈퇴 판정 + 활동량, v1.6.2 / 2026-09-08 브라우저로 재확인)

```
https://gallog.dcinside.com/<식별코드>
```

**세 갈래이고, 404가 다시 두 갈래로 나뉩니다.** 응답 본문 크기로 딱 갈립니다.

| 상태 | HTTP | 본문 | 본문 내용 |
|---|---|---|---|
| 살아있는 계정 | 200 | 16~20KB | 정상 페이지 (공개/비공개 무관) |
| **탈퇴한 계정** | 404 | **103바이트** | `_error/deleted` 로 보내는 스크립트 한 줄 |
| **없는 코드** | 404 | **1130바이트** | `404 Page Not Found` |

**둘 다 404입니다. 상태 코드로는 못 가릅니다. 본문으로만 갈립니다.**

브라우저에서는 저 스크립트가 돌아서 "삭제된 갤로그입니다" 화면이 뜹니다. 하지만 `fetch`는 스크립트를 안 돌리므로 그 문구를 절대 못 봅니다. **찾아야 할 것은 문구가 아니라 스크립트 안의 이동 주소입니다.**

이 두 갈래를 합치지 마세요. 코드를 잘못 읽었을 때 멀쩡한 사람이 탈퇴로 찍혀 명단에서 지워지는 걸 막는 마지막 안전장치입니다.

**2026-09-08 이 안전장치를 실제로 검증했습니다.** 형식이 그럴듯한 무작위 10자리 ID 4개(`zqxjkw4821` 등)를 넣어봤더니 전부 `없는 코드`(1130바이트)로 나왔습니다. 디시가 미사용 ID에 탈퇴 응답을 주는 게 아니라, **정말로 존재했다가 탈퇴한 계정에만** 줍니다. 안전장치가 의도대로 돕니다.

#### ⚠ 글·댓글 수에 천 단위 쉼표가 붙습니다

```html
<h2 class="tit" onclick="...">게시글<span class="num">(4,972)</span></h2>
<h2 class="tit" onclick="...">댓글<span class="num">(9,288)</span></h2>
```

활동이 적은 계정은 `(37)` 처럼 쉼표가 없어서, 작은 갤에서 시험하면 멀쩡해 보입니다. **v1.6.4까지 정규식이 `\((\d+)\)` 라 1,000을 넘는 순간 못 읽었습니다.** 같은 화면의 `스크랩(0)`·`방명록(14)`은 잡히니 겉으로는 "화면 구조가 바뀐 것 같다"로만 보였습니다.

**파딱 갤(t1win)처럼 큰 갤은 활동량 많은 사람이 대부분이라 이쪽이 다수입니다.** 2026-09-08 갤로그 점검에서 300명 중 258명이 실패한 원인의 상당 부분이 이것입니다. v1.6.5에서 `\(([\d,]+)\)` 로 고치고 쉼표를 떼서 읽습니다.

**비로그인 상태에서도, 비공개 갤로그여도 숫자가 나옵니다.** 목록만 `게시글이 없습니다`로 가려지고 개수는 그대로입니다. 이게 이 기능이 성립하는 근거입니다.

**디시가 차단한 갤로그 화면은 아직 못 봤습니다.** 지금은 `alive`로 분류돼 명단에 남습니다. 그쪽이 안전합니다.

### 2-5. 차단 실행 API (실제로 동작하는 유일한 경로)

```
POST https://gall.dcinside.com/ajax/managements_ajax/user_code_avoid
Content-Type: application/x-www-form-urlencoded; charset=UTF-8
X-Requested-With: XMLHttpRequest

ci_t=<토큰>&gallery_id=<갤러리ID>&_GALLTYPE_=M
&user_codes=<식별코드, 줄바꿈으로 여러 명>
&avoid_hour=744&avoid_reason=1&avoid_reason_txt=
```

- `ci_t` = **`ci_c` 쿠키 값 그대로**입니다. HTML 안에는 없습니다. 확장은 `chrome.cookies.get`으로 읽습니다.
- **⚠ `ci_c` 쿠키는 로그아웃해도 남습니다.** 2026-09-09 실측. 쿠키가 있다고 로그인된 게 아닙니다. `getCiToken()`이 성공하는 것만 보고 로그인 상태를 판단하면 안 됩니다.
- `avoid_hour`: 1 / 6 / 24 / 168 / 336 / **744(31일)**
- `avoid_reason`: 1음란성 2광고 3욕설 4도배 5혐오콘텐츠 6저작권침해 7명예훼손 0직접입력
- `user_codes`는 **500자 제한**입니다. 넘으면 나눠 보내야 합니다.
- **화면 표기와 코드 값의 공백이 다를 수 있습니다.** 목록에 `혐오콘텐츠`로 나오는데 팝업 라벨은 `혐오 콘텐츠`입니다. 그대로 들고 가면 `알 수 없는 사유`로 그룹 전체가 실패합니다. 공백을 무시하고 맞춘 뒤 아는 값으로 되돌려 씁니다.

### 2-6. 동작하지 않는 것들 (중요)

**목록 체크박스 + 하단 `차단` 버튼은 작동하지 않습니다.**
확인 버튼을 누르면 "차단되었습니다" 알림이 뜨지만 목록에 행이 생기지 않고 관리 기록에도 남지 않습니다. **브라우저에서 손으로 눌러도 동일**하므로 자동화 문제가 아니라 디시 쪽 문제로 보입니다. (2026-09-05 확인)

이것 때문에 초기 구현이 통째로 헛돌았습니다. 나중에 디시가 고치면 이 경로로 `+ IP`나 IP 대역까지 원형 그대로 재차단할 수 있게 되니, 가끔 확인해볼 가치는 있습니다.

**IP 차단은 지원하지 않습니다.** 디시는 매니저에게도 IP를 앞 2옥텟까지만 보여줍니다(`183.103.*.*`). 통신사 대역을 통째로 막는 셈이라 무고한 이용자만 막히고 정작 상대는 IP를 바꾸면 그만입니다. **의도적으로 제외한 것이며 되살리지 마세요.**

한 가지 사실을 바로잡습니다. 예전에 "직접 차단 팝업은 식별코드만 받는다"고 적어뒀는데 **틀렸습니다.** 2026-09-08 화면에서 팝업 하단에 `삭제`와 함께 **`식별 코드의 IP 차단` 체크박스**가 있는 것을 확인했습니다. 즉 화면에서는 IP 차단이 가능합니다. 우리가 그 파라미터를 안 보내기 때문에 코드 단독 차단이 되는 것이고, 이건 못 하는 게 아니라 안 하는 것입니다. 위의 이유로 앞으로도 안 합니다.

**재차단하면 항상 코드 단독 차단이 됩니다.** 원래 `+ IP`로 걸려 있던 사람도 마찬가지입니다.

### 2-7. 그 밖의 성질

- 해제된 목록은 **30일만 보관**됩니다. 그 뒤에는 추적할 방법이 없습니다.
- 재차단해도 **옛 해제됨 행이 30일간 남습니다.** "해제됨 행이 있으면 재차단" 같은 단순 규칙을 쓰면 이미 막아둔 사람을 매번 다시 막게 됩니다.
- 중복 차단하면 기존 차단이 해제되고 새로 걸립니다. 남은 기간이 초기화됩니다.
- 로그인 직후 **비밀번호 변경 캠페인 페이지**로 튕기는 경우가 있습니다.
- **요청을 많이 보내면 IP가 막힙니다. 정황이 아니라 실제로 났습니다(2026-09-08). 5-5절.** 다른 확장(갤스코프)이 "혹시모를 IP 차단 리스크"를 이유로 페이지 상한을 200으로 두었습니다. 우리는 요청 간격 300ms(1000페이지 초과 시 500ms)를 지킵니다. 완장 계정 IP가 막히면 관리 자체를 못 하니 낙관하지 마세요.

---

### 2-9. 로그인이 풀렸을 때의 응답 (2026-09-09 실측)

로그아웃 상태로 관리 화면을 부르면 **안내 문구가 아니라 스크립트 한 줄**이 옵니다.

| | |
|---|---|
| HTTP | 200 (오류가 아닙니다) |
| 본문 | **87바이트** |
| 모양 | `<script>`로 시작, `location`으로 보냄 |
| 한글 | **한 글자도 없음** |
| `minor_block_list` | 없음 |

**갤로그 탈퇴 계정(103바이트)과 같은 형태입니다.** 디시는 이런 상황을 문구가 아니라 스크립트 이동으로 처리합니다.

v1.7.1까지 `/로그인/.test(html)` 로 가렸는데 **본문에 그 글자가 없으니 절대 안 걸렸습니다.** 그래서 로그아웃인데도 `차단 목록을 읽지 못했습니다. 매니저 권한이 있는 갤러리인지 확인하세요` 가 떴습니다. 완장은 자기 권한이 날아간 줄 알고 엉뚱한 곳을 봅니다. 명단 채우기 쪽은 아예 로그인 검사가 없었습니다.

지금은 `looksLoggedOut()` 이 **짧은 스크립트 이동 본문**을 보고 가릅니다. 이동 주소는 바뀔 수 있으니 주소로 잡지 않습니다. 로그인 화면을 통째로 주는 경우를 대비해 옛 문구 검사도 남겨뒀습니다.

**교훈:** 디시는 "설명해주는 오류"를 잘 안 줍니다. 짧은 스크립트 한 줄로 때우는 자리가 여럿입니다(로그아웃, 갤로그 탈퇴). **문구를 찾지 말고 응답의 모양을 보세요.**

---

### 2-10. '중지'가 무엇인가 (자주 나오는 질문)

`enabled: false` 입니다. **조회 대상에서 빠지지만 명단에는 남습니다.**

- `runCheck`가 `watchlist.filter(t => t.enabled !== false)` 로 거릅니다
- 손으로 켜고 끌 수 있지만, **주로 자동으로 붙습니다.** 60일간 차단 이력이 안 보이면 확장이 자동 중지하고 메모에 `60일간 기록 없어 자동 중지` 를 답니다
- "차단은 풀되 블랙리스트로 남긴다"는 용도로도 맞습니다

v1.7.3에서 **`중지만 보기`** 를 넣었습니다. 그전에는 숫자만 보이고 모아볼 수가 없어서, 명단에서 뺄지 판단할 수가 없었습니다(파딱 지적).

---

## 3. 확장 구조

```
manifest.json     MV3
                  권한: storage unlimitedStorage alarms cookies notifications
                  host: https://gall.dcinside.com/*  https://gallog.dcinside.com/*
dc.js             디시 통신 + HTML 파싱 + 판정 로직 (핵심)
background.js     서비스워커. 스케줄, 조회, 재차단, 명단 채우기, 명단 정리
popup.html/css/js 화면 (탭 4개 + 명단 탭 안의 갈래 3개)
fixtures.mjs      검사들이 같이 쓰는 실제 관리 화면 마크업        ← v1.7.7
test.mjs          파싱·판정 검사. `node test.mjs`
test-popup.mjs    화면 동작 검사. `node test-popup.mjs` (설치할 것 없음)
test-bg.mjs       재차단 실행 경로 검사. `node test-bg.mjs`      ← v1.7.7
icon16/48/128.png
README.md PRIVACY.md STORE.md 설치법.txt .gitignore handover.md
```

**`popup.js` 는 v1.7.5부터 `dc.js` 를 import 합니다.** 갤로그 기록 합치기 규칙(`mergeGallog`)을 가져다 씁니다. `popup.html` 의 `<script>` 에 `type="module"` 이 있어서 됩니다. 옛 주석 중에 "popup.js 는 dc.js 를 가져오지 않는다"는 말이 남아 있으면 그건 틀린 것입니다.

화면 탭은 **할 일 / 명단 / 재차단 이력 / 설정** 네 개입니다 (v1.6.7). `명단` 탭 안에서 **보기 · 채우기 · 빼기**를 고릅니다.

```
할 일        재차단 후보, 완장이 직접 풀어준 사람, 작업 기록
명단         보기(=옛 영구 관리 명단) / 채우기 / 빼기(=옛 명단 정리)
재차단 이력  옛 '이력' 탭
설정         기본 + 고급(접힘)
```

**id는 개편 전 그대로 두었습니다.** `test-popup.mjs`가 `popup.html`에 실제로 있는 id로 가짜 화면을 만들기 때문에, id를 유지하면 화면을 바꿔도 `test-popup.mjs` 검사가 그대로 돕니다. 앞으로 화면을 손볼 때도 **id는 건드리지 말고 배치·이름만** 바꾸세요. 탭 전환은 `.tab`/`.panel`, 갈래 전환은 `.seg`/`.subpanel`로 서로 분리돼 있습니다.

용어는 `식별코드` 하나로 통일했습니다. 예전엔 `식별자`·`식별 코드`·`식별코드`가 섞여 있었고, 지금은 섞이면 검사가 실패합니다.

**MV3 서비스워커에는 DOMParser가 없습니다.** 그래서 HTML 파싱을 정규식으로 합니다. 뽑을 게 표 몇 칸뿐이라 충분하지만, 디시가 마크업을 바꾸면 여기가 먼저 깨집니다.

### 3-1. dc.js 주요 함수

| 함수 | 역할 |
|---|---|
| `parseBlockList(html)` | 차단 목록 → 행 배열 (+ `expectedRows`/`missedRows`) |
| `analyzeCode(rows, code, entry, now)` | 판정. `candidate`/`manual`/`active`/`none` + `nextCheckAt` |
| `fetchRowsForCode(gid, code)` | 식별코드로 검색 조회 |
| `crawlList(gid, maxPages, onPage, opts)` | 차단 목록 순회 (`p` 파라미터) |
| `collectByDuration(gid, '31일', pages, onProgress, untilDate)` | 명단 채우기용 수집 |
| `blockCodes(gid, codes, reason, hours, onProgress)` | 재차단 + 검증 + 하루 한도 감지 |
| `parseBoardList(html)` | 갤 글 목록 → `{uid, ip, nick, stamp, day}` 배열 |
| `collectActivity(gid, maxPages, onProgress, untilDate)` | 글쓴이별 마지막 글 날짜 수집 (`page` 파라미터) |
| `checkGallog(code)` | `deleted` / `notfound` / `alive` / `other` / `error` |
| `chunkCodes(codes)` | 500자 단위 분할 |
| `expiresAt(row)` / `isManualRelease(row)` | 만료 시각 계산 / 수동 해제 판정 |
| `rowHealth(rows)` | 무엇을 못 읽었는지 종류별로(`missed`/`badState`/`badIdentity`) |
| `carryOver(prev, checked, watched)` | 이번에 조회 안 한 사람의 옛 판정 유지 |
| `mergeGallog(mine, theirs)` | 갤로그 기록 두 벌 합치기 (v1.7.5). 안 넓어지면 `null` |
| `pickGallogTargets(...)` | 갤로그 점검 대상 고르기 (v1.7.1에 `background.js`에서 옮겨옴) |
| `jitter(base, spread)` | 요청 간격 흔들기 |
| `listPagesFor(n)` / `isBusy(status, now)` / `localDateKey(d)` | 공용 헬퍼 |
| `CHECK_DELAY_MS` / `GALLOG_DELAY_MS` (1200) / `FETCH_SECS` (0.5) | 간격, 그리고 한 명당 실제 조회 시간 |

**`FETCH_SECS` 를 빼먹지 마세요 (v1.7.4).** 예상 시간을 간격만으로 계산하면 늘 모자랍니다. 300명에 `6분쯤`이라 해놓고 8~9분이 걸려서 완장이 멈춘 줄 알았습니다. `popup.js` 에도 같은 식이 **두 군데** 있고, 검사가 전부 대조합니다(`match` 는 첫 번째만 봐서 v1.7.7에서 `matchAll` 로 고쳤습니다).

**공용 헬퍼는 dc.js 한 곳에만 두고 background.js가 가져다 씁니다.** 양쪽에 같은 식을 적어두면 한쪽만 고쳤을 때 테스트가 눈치채지 못합니다. v1.5.6에서 정리했습니다.

`parseBlockList`가 돌려주는 배열에는 `expectedRows`(표에 있던 행 수)와 `missedRows`(그중 못 읽은 수)가 붙어 옵니다. 빈 결과 자체는 정상일 수 있으므로(이력 없는 코드 검색) 이 대조 없이는 파싱이 깨진 걸 알 수 없습니다.

### 3-2. 판정 규칙 (analyzeCode)

**가장 최근 행 하나만 보고 판단합니다.** 옛 행은 재차단 흔적이나 중복 차단으로 일찍 해제된 흔적이라 오판의 원인이 됩니다.

```
차단 중인 행이 있음        → active    (다음 확인 = 그 행의 만료 시각 + 1분)
최신 행이 만료 전에 해제됨 → manual    (완장이 직접 푼 것. 재차단 안 함. 7일 뒤 재확인)
최신 행이 만료돼서 해제됨  → candidate (재차단 후보)
행이 아예 없음             → none      (1일 뒤 재확인, 60일 넘으면 자동 중지)
```

`manual` 판정은 `처리시각 + 차단기간`을 계산해 **예정보다 10분 이상 일찍** 풀렸으면 사람이 푼 것으로 봅니다. 신문고 민원으로 풀어준 사람을 12시간 만에 다시 막는 사고를 방지하기 위한 장치입니다.

#### ⚠ `entry` 를 반드시 넘기세요

```js
analyzeCode(rows, code, entry, now)
                        ^^^^^ 빼면 candidate 갈래에서 터진다
```

`candidate` 를 만들 때 마지막 줄이 `entry.reason || latest.reason || "음란성"` 을 읽습니다. **`entry` 가 없으면 `Cannot read properties of undefined (reading 'reason')` 입니다.**

하필 `candidate` 갈래에서만 터집니다. `active` · `manual` · `none` 은 `entry` 를 안 보고 먼저 돌아가므로, **재차단할 사람이 없을 때는 멀쩡히 지나갑니다.** v1.7.6의 `실행 전에 후보를 다시 확인`이 정확히 이 모양으로 나갔습니다. 0절을 보세요.

명단 항목이 없는 자리에서 부를 때도 `{}` 나 후보 객체를 넘기세요. 후보 객체는 `reason` · `memo` 를 들고 있어서 그대로 넘기면 사유 승계까지 유지됩니다.

### 3-3. 대량 명단 대응

명단이 수만 명이 될 수 있습니다. 한 명당 조회 한 번이므로 그대로 두면 감당이 안 됩니다.

**만료 예정 시각을 기억해두고 그때까지 조회하지 않습니다.** 조회량이 명단 크기가 아니라 그날 만료되는 인원에 비례합니다.

| 명단 | 하루 조회 |
|---|---|
| 1,000명 | 약 30회 |
| 10,000명 | 약 320회 |
| 100,000명 | 약 3,200회 |

- 한 번에 조회할 인원 상한: `maxChecksPerRun` (기본 300)
- 예약이 어긋날 수 있으므로 매 확인마다 오래 확인 안 한 사람을 몇 명씩 섞어 봅니다: `sweepPerRun` (기본 20)
- 즉시 전원 재확인: 명단 탭의 `전체 다시 확인`
- 60일간 기록 없으면 자동 중지

### 3-4. 명단 채우기

차단 목록을 훑어 31일 차단자를 모읍니다. 최대 3000페이지(90,000행).

**`이 날짜까지`를 쓰는 게 낫습니다.** 페이지 수를 감으로 찍지 않아도 되고 필요 이상으로 훑지 않습니다.

**v1.6.1부터 날짜를 넣으면 날짜가 기준입니다.** 그전에는 페이지 수와 날짜가 둘 다
걸려서, 페이지 입력란 기본값이 10이라 날짜를 넣어도 10페이지에서 멈췄습니다.
상한 경고는 떴지만 날짜를 지정한 사람 눈에는 왜 덜 훑었는지 보이지 않았습니다.
지금은 날짜를 넣으면 페이지 입력란이 흐려지고 상한은 3000 안전장치로만 남습니다. 경계를 지났는지 알려면 기준일보다 오래된 행이 나오는 페이지까지 읽어야 하므로 한 페이지를 더 읽는데, 그 페이지의 기준일 밖 행은 담지 않습니다.

**사유 필터**가 있습니다. 불러온 뒤 사유 칩으로 추려서 담습니다. 서버에 다시 묻지 않고 화면에서 거릅니다.

주의 두 가지입니다.

- **칩을 전부 해제한 상태를 '전체'로 되돌리지 마세요.** 사용자는 아무것도 안 고른 줄 아는데 화면에는 전원이 떠서, 그대로 `전체 선택 → 추가`를 누르면 엉뚱한 사람이 명단에 들어갑니다. 지금은 0명을 보여주고 안내 문구를 띄웁니다.
- **칩 상태를 화면에서 읽지 마세요.** `render()`가 체크박스를 새로 만들기 때문에 방금 교체된 요소를 보게 됩니다. `scanReasons` 상태에서 더하거나 빼는 방식으로 계산합니다.

### 3-5. 하루 차단 한도 (v1.5.7)

디시는 완장 계정마다 하루 차단 횟수에 한도를 둡니다. **한도에 걸리면 그 뒤 요청은 전부 헛수고이므로 즉시 멈춥니다.**

- 남은 묶음을 보내지 않고 중단
- **보내지도 않은 사람을 '실패'로 기록하지 않습니다.** 시도조차 안 한 걸 실패로 적으면 나중에 이력을 못 믿게 됩니다.
- 못 보낸 사람은 후보 목록에 그대로 남깁니다
- 디시가 뭐라고 답했는지 원문을 기록에 남깁니다

**2026-09-08 실제 문구를 확인했습니다.**

```
일일 차단 횟수가 초과되어 장시간 차단이 불가능합니다.
```

여기서 두 가지가 드러났고, 둘 다 이전 서술을 뒤집습니다.

- **매니저(주딱)에게도 한도가 있습니다.** "주딱 계정에서는 400번을 해도 안 걸렸다 → 부매니저만 한도가 있는 것으로 추정"이라고 적혀 있었는데 틀렸습니다. 400번에서 안 걸렸던 건 그날 한도에 못 미쳤던 것으로 보입니다.
- **막히는 건 '장시간'뿐입니다.** 한도에 걸려도 1시간·6시간은 계속 걸립니다. 우리는 31일만 걸므로 결국 전부 막힌 것과 같아서 즉시 중단이 맞습니다. 다만 완장 입장에서는 급한 분탕을 짧게라도 막을 길이 남아 있으므로, 한도에 걸리면 기록에 그렇게 안내합니다.

`dc.js`는 확인된 문구를 `RE_DAILY_LIMIT_KNOWN`으로 먼저 정확히 잡고, 문구가 바뀌어도 놓치지 않게 넓은 `RE_DAILY_LIMIT`를 한 겹 더 둡니다. **놓치는 쪽이 훨씬 나쁩니다.** 못 알아채면 남은 인원을 전부 보내고 전부 실패로 적습니다. 오탐은 안 보내고 후보에 남기므로 안전한 방향입니다. `test.mjs`의 [10-2]가 한도 문구와 한도가 아닌 응답을 갈라놓았습니다.

**2026-09-08 실전에서 그대로 돌았습니다.** 주딱 계정 `90_00_memory`에서 후보 2명에 재차단을 걸었더니 첫 묶음부터 한도에 걸렸고, `RE_DAILY_LIMIT_KNOWN`이 정확히 잡았습니다(넓은 예비 정규식이 아니라 정밀 패턴 쪽입니다). 남은 묶음을 안 보냈고, 실패로 적지 않았고, 후보 2명을 그대로 남겼습니다. **차단 목록을 직접 대조해 실제로 아무 행도 안 생긴 것까지 확인했습니다.**

다만 그때 문구 두 줄이 잘못 나왔습니다. v1.6.5에서 고쳤습니다.

```
→ 0건 모두 31일 차단 확인됨.     ← 한 명도 안 보냈는데 성공처럼 읽힌다
완료: 0/0건 확인
```

`blockCodes`가 `attempted.length === 0` 인 경우를 안 갈라놨고, `runApply`도 `records.length === 0` 을 성공으로 셌습니다. 지금은 각각 `한 명도 보내지 못했습니다` / `재차단된 사람이 없습니다. 후보 N명은 그대로 남아 있습니다` 로 나옵니다. 기존 테스트가 **두 번째 묶음에서 걸리는 경우만** 봐서 이 자리가 비어 있었습니다. 첫 묶음부터 걸리는 경우를 [10]에 추가했습니다.

### 3-6. 완장끼리 후보 나누기 (v1.5.7)

확장에는 서버가 없습니다. 사람이 옮겨야 합니다. 두 경로를 둡니다.

**복사** — 사유별로 정리된 코드 목록이 클립보드에 들어갑니다. 갤러리 게시글에 그대로 올릴 수 있습니다.

```
# 재차단 후보 3명 (2026-09-07)

# 음란성 2명
aaa0001
aaa0002
```

**파일** — JSON으로 내보내고 불러옵니다. 사유가 파일에 들어 있어 받는 쪽이 다시 고르지 않아도 됩니다.

**v1.6.1부터 파일이 두 갈래입니다.** 용도가 다르기 때문입니다.
후보 내보내기는 남에게 넘기는 것이라 코드와 사유만 담고, 명단 내보내기는
본인 백업이라 메모와 사용 여부까지 담습니다. 파딱이 확장을 지웠다 다시 깔면서
명단을 복원했는데 메모가 통째로 날아간 것이 계기였습니다. 백업인데 메모가
빠지면 백업이라고 할 수가 없습니다. 명단 파일에는 메모가 들어가므로 내보낼 때
공개된 곳에 올리지 말라고 한 번 물어봅니다.

```json
{ "kind": "dcblock-share", "what": "candidates", "formatVersion": 1,
  "gallery": "90_00_memory", "count": 3,
  "items": [ { "code": "aaa0001", "reason": "음란성" } ] }
```

**디시 게시글에는 파일 첨부가 안 됩니다.** 이미지만 됩니다. 갤에 공개로 올릴 때는 복사, 완장끼리 직접 주고받을 때는 파일입니다. 완장끼리 친목이 금지된 갤러리가 있어서 공개 경로가 필요합니다.

**복사본과 후보 파일은 식별 코드와 사유만 담습니다.** 닉네임과 메모는 넣지 않습니다.
명단 파일(백업)만 메모를 담습니다.

파일에 우리가 모르는 사유가 있으면 `음란성`으로 넣되 **몇 명이 그렇게 됐는지 알려줍니다.**
조용히 바꾸면 나중에 그 사유로 재차단됩니다.

받는 쪽은 명단 탭 `여러 명 한 번에`에 글을 통째로 붙여넣어도 됩니다. 코드처럼 생긴 낱말만 골라냅니다. 식별 코드는 지금까지 본 것이 전부 영문+숫자 조합(`capture6180`, `chip3298`, `leaf4517`)이라 **둘 다 들어 있어야** 코드로 봅니다. 이 조건이 없으면 `2026년 9월 7일`에서 `2026`을, URL에서 `https`를 코드로 집습니다.

### 3-7. 명단에서 빼기 (옛 '명단 정리', v1.6.0 / v1.6.7에서 이름 변경)

명단이 커지면 이미 떠난 사람이 계속 남습니다. 두 가지로 걸러냅니다.

**활동 점검** — 갤 글 목록을 훑어 최근에 글 쓴 식별코드를 모읍니다. 한 번에 전원 판정이라 명단이 커져도 요청이 안 늘어납니다. 하루 100글 갤러리면 3개월치가 94페이지입니다.

**갤로그 점검** — 탈퇴 계정을 찾습니다. 한 명당 요청 한 번이라 느립니다. 활동 점검으로 대상을 줄인 뒤에 돌리는 게 좋습니다. 오래 확인 안 한 사람부터 돌고, 이미 탈퇴로 확인된 사람은 다시 안 봅니다.

결과는 근거가 확실한 순서로 정렬됩니다.

| 표시 | 뜻 | 근거 |
|---|---|---|
| 탈퇴 | 갤로그가 `/_error/deleted` | 확실 |
| 코드 확인 필요 | 갤로그가 404 | 코드를 잘못 읽었을 수 있음 |
| 갤로그 N일째 그대로 | 디시 전체 글·댓글 수 변동 없음 | 셈. 댓글까지 본다 (v1.6.2) |
| 글 없음 | 목록에 안 나옴 | 약함. 댓글만 다는 사람도 걸림 |

**어느 쪽도 자동으로 지우지 않습니다.** 지우기 전 확인창에서 탈퇴가 확인되지 않은 사람이 몇 명 섞여 있는지 알려줍니다.

**끝까지 못 갔으면 판정을 남기지 않습니다.** 페이지 상한에 걸리거나 `page` 파라미터가 안 먹으면, 덜 훑고 "글 없음"이라 하는 게 되니 아예 기록하지 않습니다.

### 3-8. 저장 데이터 (chrome.storage.local)

```
settings   { galleryId, checkTimes[], maxPerRun, maxChecksPerRun,
             sweepPerRun, autoApply, notify,
             recheckBeforeApply }                                ← v1.7.6
watchlist  [{ kind:'code', value, reason, memo, enabled,
              nextCheckAt, lastVerifiedAt, lastSeen, missingSince,
              lastPostAt, noPostSince, activityCheckedAt,      ← v1.6.0
              gallogState, gallogCheckedAt,                    ← v1.6.0
              gallogPosts, gallogComments,                     ← v1.6.2
              gallogTotal, gallogSince,                        ← v1.6.2
              gallogCountedAt }]                               ← v1.6.5
candidates [] 재차단 후보
candidatesAt  그 후보를 판정한 시각. 실행 전에 낡았는지 본다       ← v1.7.6

**`nextCheckAt: 0` 은 '만료 시각을 모른다'는 뜻이고, 모르면 매번 조회 대상이 됩니다.** 명단 채우기로 담는 경로가 v1.7.7까지 이 값을 안 넣어서, 파딱 갤 4577명이 전원 0이었습니다. 3-3절의 설계(만료까지 조회하지 않는다)가 그 경로에만 통째로 빠져 있었던 것입니다. v1.7.8부터 담을 때 차단 목록의 처리 시각과 기간으로 계산해 넣습니다. **옛 명단은 채우기를 다시 돌려 담기를 눌러야 채워집니다.**
manual     [] 완장이 직접 풀어준 것으로 보이는 대상
imports    [] 명단 채우기 결과
importsAt  명단 채우기를 마친 시각 (팝업이 새 결과인지 알아보는 데 씀) ← v1.6.5
history    [] 재차단 이력
logs       [] 기록 창 내용 (최근 200개)
status     { text, busy, busySince }
```

**`gallogCheckedAt`과 `gallogCountedAt`은 다릅니다.** 앞은 갤로그를 열어본 시각, 뒤는 **숫자를 실제로 읽어낸** 시각입니다. 정렬은 뒤엣것으로 합니다. 앞엣것으로 정렬하면 숫자를 못 읽은 사람도 '방금 봤다'가 되어 뒤로 밀리고, 영영 다시 안 보게 됩니다. 12시간 재확인 간격은 앞엣것으로 봅니다.

**후보·수동해제 목록을 통째로 덮어쓰지 마세요.** 이번에 조회하지 않은 사람의 판정이 조용히 사라집니다. `carryOver()`를 쓰세요. 실제로 '완장이 직접 해제' 목록과 하루 한도로 남겨둔 후보가 이렇게 없어졌습니다.

**chrome.storage는 Date 객체를 담지 못합니다.** 빈 값으로 바뀌어 `Invalid Date`가 됩니다. 시각은 반드시 숫자(`getTime()`)로 저장하세요.

**`busy` 잠금에는 `busySince`가 같이 붙습니다.** MV3 서비스워커는 작업 도중에도 종료되는데, 그러면 `busy`가 true인 채 남아 이후 알람이 전부 되돌아가고 팝업 버튼도 굳습니다. 확장이 조용히 영구 정지합니다. 30분이 지난 잠금은 무시하고, 브라우저 재시작 시 풀며, 긴 작업 중에는 10~25회마다 `touchBusy()`로 갱신합니다.

**사람당 한 요청이 나가는 곳은 전부 잠그세요.** v1.7.6의 재확인 루프가 잠금 밖에 있었습니다. 도는 동안 알람이 정기 확인을 띄우면 두 경로가 겹쳐 돕니다. **요청이 두 배로 나가면 IP 차단을 피하려고 넣은 1.2초 간격이 의미가 없어집니다.** 메시지 핸들러가 `isBusy` 를 보고 막아주지만, 그건 사람이 버튼을 누를 때뿐입니다. 알람은 그 검사를 안 거칩니다.

---

## 4. 반드시 지켜야 할 원칙

이 프로젝트에서 실제로 사고가 났거나 날 뻔했던 것들입니다.

**1. 서버 응답을 믿지 마세요.**
디시는 아무 일도 하지 않고 "차단되었습니다"라고 응답합니다. 실제로 이것 때문에 두 번이나 "성공했다"고 잘못 보고했습니다. 차단 후 **목록을 다시 읽어 31일 행이 실제로 생긴 것만** 성공으로 기록해야 합니다.

**2. 조용한 실패를 만들지 마세요.**
이 프로젝트의 버그는 거의 전부 이 형태였습니다.

- 검색 강조 태그 때문에 식별자를 못 읽어 아무도 재차단되지 않음
- 페이저를 엉뚱한 걸 잡아 1페이지만 불러옴
- 표를 한 행도 못 읽으면 결과가 빈 배열이라 '이력 없음'과 구분 안 됨
- `blockstate` 클래스를 못 잡으면 전원이 '차단 중'으로 보임

**뭔가 못 읽었으면 반드시 눈에 보이게 남기세요.** 행 수를 세서 대조하고, 페이지를 넘겼는지 기록에 남기고, 상태를 못 읽은 행도 실패로 세십시오.

**3. 자동으로 차단하거나 삭제하지 마세요.**
후보만 보여주고 사용자가 버튼을 눌러야 실행됩니다. 명단 정리도 마찬가지입니다.

**4. 명단에 등록된 대상만 갱신하세요.**
"만료된 차단을 전부 자동 갱신"은 절대 금지입니다.

**5. 한도를 두세요.**
후보가 `maxPerRun`(기본 100)을 넘으면 실행하지 않고 멈춥니다.

**6. 서버에 부담을 주지 마세요.**
조회는 하루 1~2회. 요청 사이 300~400ms 간격. IP 차단 위험이 실재합니다.

**7. 화면 구조를 추측하지 마세요.**
v1.5.2에서 `blockstate` 클래스에 접미사가 붙는다고 추측했는데 실제로는 두 번째 클래스(`blockstate txtbtn`)였습니다. v1.5.3에서 페이지 파라미터를 `page`로 짐작했는데 실제로는 `p`였고, 그대로 냈으면 여전히 안 됐을 겁니다. 둘 다 실제 HTML을 받아보고서야 드러났습니다. **표 하나 복사해달라고 부탁하는 게 몇 시간을 아낍니다.**

**9. 긴 작업 중에는 chrome API 를 20초 안에 한 번씩 부르세요.** (v1.7.8에서 추가)
MV3 서비스워커는 **30초 동안 chrome API 호출이 없으면 종료됩니다.** `fetch` 와 `setTimeout` 은 그 타이머를 되살리지 못합니다. 요청 사이를 1.2초씩 쉬면서 도는 이 프로젝트의 모든 루프가 여기에 걸립니다.

죽는 방식이 최악입니다. 예외도 안 나고 기록도 안 남습니다. 작업이 그냥 중간에서 사라지고, 완장이 보기엔 "진행 기록이 어느 순간 멈춰 있다"가 전부입니다. 원칙 2번이 말하는 조용한 실패의 가장 지독한 형태입니다.

`setStatus(text, true)` 가 keepAlive 를 겁니다. **긴 작업은 반드시 `setStatus` 로 잠그고 시작하세요.** 잠그지 않고 도는 루프는 워커와 함께 죽습니다 (v1.7.6의 재확인 루프가 그랬습니다).

**그리고 긴 작업은 결과를 중간에 저장하세요.** keepAlive 가 있어도 브라우저 종료·절전·크래시로 죽을 수 있습니다. 끝에 한꺼번에 저장하면 죽을 때 통째로 사라지고, **다음 차례에 같은 자리에서 또 죽어 영원히 진행되지 않습니다.**

**8. 소스에 글자가 있는지 보는 검사는 동작 검사가 아닙니다.** (v1.7.7에서 추가)
`test.mjs`에는 원본 파일을 문자열로 읽어 대조하는 검사가 여럿 있습니다. 그건 **어긋남**을 잡는 용도입니다 — 상수는 고쳤는데 코드가 리터럴을 쓴다든가, 코드는 고쳤는데 화면 문구가 옛 값을 말한다든가. 그 자리에서는 잘 듣습니다.

**그런데 그것으로 새 기능이 도는지를 확인하면 안 됩니다.** v1.7.6의 재확인 기능은 `if (settings.recheckBeforeApply)` 가 소스에 있는지, 중단 문구가 있는지를 정규식으로 보는 검사 두 개를 통과하고 나갔고, 켜는 순간 첫 후보에서 죽었습니다. 검사는 글자가 있다고만 말해줬지 그 글자가 도는지는 말해주지 않았습니다.

**기능을 넣었으면 그 기능을 돌려보는 검사를 넣으세요.** `test-bg.mjs` 처럼 가짜 서버를 세우고 원본을 그대로 실행하면 됩니다. 설치할 것은 없습니다. 그리고 **넣기 전에 일부러 되돌려서 진짜 실패하는지 확인하세요.** 통과하는 것만 보고 안심하면 아무것도 안 지켜집니다.

---

## 5. 미해결 / 다음에 할 일

### 5-1. 재차단 — 2026-09-09 실전 성공 ✅

**이 프로그램의 본체이고, 실전에서 성공했습니다.** 오래 미해결로 남아 있던 항목입니다.

#### 2026-09-09 주딱 갤 `90_00_memory` 결과

`capture6180` `chip3298` 두 명. 어제 1시간 차단이 자연 만료된 상태였습니다. 하루 차단 한도가 0시에 풀린 뒤 `재차단 실행` 한 번으로 끝났습니다.

**관리 화면 차단 목록에서 눈으로 확인한 것** — 기록이 아니라 이쪽이 근거입니다.

| 번호 | 닉네임(식별코드) | 사유 | 기간 | 처리일 | 상태 |
|---|---|---|---|---|---|
| 604 | ㅇㅇ (`chip3298`) | 음란성 | **31일** | 2026.09.09 | **차단 중** |
| 603 | 성소피아대성당 (`capture6180`) | 음란성 | **31일** | 2026.09.09 | **차단 중** |

같이 확인된 것들입니다.

- **사유가 이어졌습니다.** 바로 아래 602·601번(1시간·음란성·해제됨)의 사유를 그대로 물려받았습니다. 새로 고르거나 기본값으로 덮지 않았습니다
- **`직접 차단`으로 들어갔습니다.** 게시글에 딸린 차단이 아니라 API로 건 것이라 정상입니다(2-5절)
- 두 명 모두, 한 명도 빠지지 않았습니다
- 재차단된 사람은 `할 일` 탭에서 빠지고 `재차단 이력`에 2건이 남았습니다

**전날(09-08)에는 하루 차단 한도에 걸려 한 명도 못 보냈습니다.** 그때도 확장은 "못 보냈다"고 정확히 보고했고, 차단 목록을 대조해 실제로 아무 행도 안 생긴 것까지 확인했습니다(3-5절). 한도 처리와 실제 전송이 둘 다 맞게 도는 것이 이틀에 걸쳐 증명된 셈입니다.

#### 다음 확인 — 2026년 10월 10일

위 재차단으로 두 사람에게 31일 시계가 켜졌습니다. **10월 10일이 주딱 갤의 진짜 31일 만료일입니다.** 그날 자연 만료 → 후보 재등장 → 재차단까지 보면 한 바퀴가 완전히 닫힙니다.

조건은 하나입니다. **그때까지 두 사람을 손으로 풀거나 다시 차단하지 마세요.** 만료 10분 이상 전에 완장이 직접 풀면 `manual`로 판정되어 재차단하지 않습니다(정상 동작입니다).

#### ⚠ 확장 데이터가 자꾸 날아갑니다

2026-09-08과 09-09 이틀 연속으로 명단과 후보가 통째로 사라졌습니다. **원인은 확장 ID 변경입니다.** 새 폴더에 압축을 풀어 `압축해제된 확장 프로그램을 로드`로 다시 추가하면 크롬이 다른 확장으로 봅니다.

**반드시 원래 폴더에 덮어쓰고 `chrome://extensions`에서 새로고침(↻)만 하세요.** 파일을 줄 때 이 문장을 같이 적어주세요.

날아가도 복구는 됩니다. 디시가 해제됨 기록을 30일 보관하므로, 명단에 코드를 다시 넣고 `지금 확인`을 누르면 판정이 그대로 나옵니다. 잃는 것은 갤로그 글·댓글 수 기록(변동 비교의 기준점)입니다.

#### 파딱 갤 9월 9일 만료

**2026년 9월 9일**에 파딱 갤러리의 8월 9일자 31일 차단이 만료됩니다. **실제 31일 만료를 보는 첫 기회**라 주딱 갤의 1시간짜리 시험과는 별개로 의미가 있습니다.

파딱에게 확인받을 것:

1. 그 사람들의 식별코드가 **영구 관리 명단에 들어가 있는지** (명단에 없으면 아무 일도 안 일어납니다)
2. 만료 전에 **손대지 말 것** (직접 해제하면 `manual`로 판정되어 재차단하지 않습니다. 정상 동작입니다)
3. 만료 후 `지금 확인` → 후보 탭 → `재차단 실행`
4. **관리 화면에서 눈으로 확인**
5. 파딱 계정은 부매니저라 **하루 한도가 더 빡빡할 수 있습니다.** 한도에 걸리면 후보에 남으니 다음 날 이어서 하면 됩니다

### 5-2. 하루 차단 한도 문구 (2026-09-08 해결)

실제 문구를 확인했습니다. 3-5절을 보세요. `RE_DAILY_LIMIT_KNOWN`이 그 문구를 잡습니다.

남은 것은 **한도가 몇 번인지**입니다. 숫자를 알면 "오늘은 N명까지"를 미리 알려줄 수 있지만, 모른다고 지금 동작이 틀리지는 않습니다. 급하지 않습니다.

### 5-3. 디시가 차단한 갤로그 화면

파딱이 "'해당 갤로그는 ~로 인해 차단되었습니다' 로 뜬다"고 전했지만 실물을 못 봤습니다. 지금은 `alive`로 분류돼 명단에 남습니다.

화면을 구하면 `checkGallog`에 한 줄 추가로 끝납니다. 지금 구조가 "삭제만 잡고 나머지는 안 건드림"이라 붙이기 쉽습니다.

### 5-4. 갤로그 글·댓글 수로 비활성 판정 (v1.6.2 구현 완료)

파딱이 요청한 것으로, 지금의 활동 점검과 **다른 기능**입니다.

| | 활동 점검 | 갤로그 활동 |
|---|---|---|
| 보는 곳 | 해당 갤 글 목록 | 갤로그의 글·댓글 수 |
| 범위 | 이 갤에 쓴 **글**만 | 디시 전체, 댓글 포함 |
| 뜻 | "이 갤에서 안 떠든다" | "디시를 떠났다" |
| 비용 | 명단이 몇 명이든 목록 1회 훑기 | 갤로그 점검에 얹혀서 0 |

**요청이 안 늘어납니다.** `checkGallog`가 이미 갤로그 홈을 열고 있었는데 `res.url`만 보고 본문을 버렸습니다. 이제 본문에서 숫자를 같이 읽습니다.

### 2026-09-08 실제 확인한 것

```html
<h2 class="tit" onclick="location.href='/hear9577/posting';">게시글<span class="num">(29)</span></h2>
<h2 class="tit" onclick="location.href='/hear9577/comment';">댓글<span class="num">(332)</span></h2>
```

**비로그인 상태에서도, 비공개 갤로그여도 숫자가 나옵니다.** 목록만 `게시글이 없습니다`로 가려지고 개수는 그대로입니다. 이게 이 기능이 성립하는 근거입니다. 안 그랬으면 비공개인 사람을 전부 빼야 했습니다.

`test.mjs`의 `GALLOG_HOME` 픽스처가 그 실제 마크업입니다.

### 설계

`watchlist` 항목에 네 개가 붙습니다.

```
gallogPosts      게시글 수
gallogComments   댓글 수
gallogTotal      두 개의 합. 이걸로 변동을 판단한다
gallogSince      이 숫자를 처음 본 시각
```

숫자가 같으면 `gallogSince`를 건드리지 않습니다. 그래서 "며칠째 그대로인지"가 저절로 쌓입니다. 히스토리를 따로 저장할 필요가 없습니다.

**줄어든 것도 변동으로 봅니다.** 글을 지운 것도 활동한 흔적이므로 명단에 남깁니다. 안전한 방향입니다.

**숫자를 못 읽으면 0이 아니라 `null`입니다.** 0으로 읽고 "활동 없음"이라 하면 그게 바로 원칙 2번의 조용한 실패입니다. 못 읽은 사람 수는 기록과 알림에 남습니다.

### 알아둘 것

- **첫 점검에서는 아무 판정도 안 나옵니다.** 비교할 이전 숫자가 없기 때문입니다. 두 번째 점검부터 뜻이 생깁니다. 시간이 쌓여야 쓸모가 생기는 기능이라 파딱에게 미리 말해두세요.
- 기준 기간은 `명단 → 빼기`의 `최근 N개월` 선택을 활동 점검과 같이 씁니다.
- 근거 세기 순서는 탈퇴 > 코드 확인 필요 > 갤로그 변동 없음 > 이 갤에 글 없음 입니다. 삭제 확인창이 이 구분을 그대로 알려줍니다.

### 5-5. IP 차단 — 실제로 났습니다 ⚠

**2026-09-08, 파딱 계정에서 실제로 발생했습니다.** 지금까지 "정황이 있다"고만 적혀 있던 위험이 현실이 된 것이라, 이 절을 반드시 읽고 손대세요.

#### 무슨 일이 있었나

갤로그 점검 300명을 **400ms 고정 간격**으로 돌렸습니다. 기록은 이렇게 남았습니다.

```
[14:34:14] 갤로그 점검: 300명을 확인합니다. 한 명당 한 번씩 요청합니다.
[14:36:59] 탈퇴 0명, 코드 확인 필요 0명, 정상 300명, 판단 불가 0명
[14:36:59] 42명의 글·댓글 수를 기록했습니다.
[14:36:59] [경고] 258명은 갤로그가 열렸는데 글·댓글 수를 읽지 못했습니다.
           갤로그 화면 구조가 바뀐 것 같습니다.
```

그 직후 파딱이 겪은 것입니다.

- 크롬에서 디시에 들어가면 **하얀 화면**. 갤로그만이 아니라 디시 전체
- 다른 사이트는 정상. **같은 와이파이의 핸드폰도 정상**
- 크롬과 PC를 모두 재시작해도 동일. 최소 30분 지속
- **IP를 바꿔서 해결**

#### 여기서 배울 것 두 가지

**1. 디시는 IP 단위로 막습니다.** 계정이 아닙니다. 같은 회선의 다른 기기도 같이 막히고, 로그아웃해도 소용없습니다. 완장 본인이 갤러리 관리를 아예 못 하게 되므로, 이 확장이 낼 수 있는 사고 중 가장 나쁜 축입니다.

**2. 258명 실패의 원인은 하나가 아니었습니다.** 2026-09-08 브라우저로 직접 재보고서야 알았습니다.

- **천 단위 쉼표**(2-4절). 글이나 댓글이 1,000을 넘으면 못 읽었습니다. 큰 갤에서는 이쪽이 다수입니다.
- **IP 차단.** 그 뒤로 아예 응답이 안 왔을 것입니다.

확장은 이걸 전부 `화면 구조가 바뀐 것 같습니다` 한 줄로 보고했습니다. 원인이 둘인데 한 문장으로 뭉개버린 것입니다.

**여기서 배울 것:** 처음에는 저 258명을 전부 IP 차단 탓으로 봤습니다. 그럴듯했고, 시간대도 맞아떨어졌습니다. **그대로 믿었으면 진짜 버그인 쉼표를 못 찾았을 것입니다.** 실제 갤로그를 열어 숫자를 눈으로 보고서야 드러났습니다. 원칙 7번(화면 구조를 추측하지 마세요)이 여기에도 그대로 적용됩니다. 실패 원인도 추측하지 말고 실물을 보세요.

#### v1.6.5에서 한 것

| | 전 | 후 |
|---|---|---|
| 요청 간격 | 400ms 고정 | 1200ms ± 40% 난수 |
| 기본 인원 | 300명 | 50명 |
| 연속 실패 | 끝까지 계속 요청 | **5회 연속이면 중단** |
| 실패 보고 | "화면 구조가 바뀐 것 같다" | 응답 본문 크기로 갈라서 안내 |
| 재확인 | 누를 때마다 전원 | 12시간 안에 본 사람은 건너뜀 |

간격을 흔드는 이유는 평균 속도보다 **일정함 자체가 신호**이기 때문입니다. 사람은 400ms마다 정확히 클릭하지 않습니다.

중단 판단은 **행동으로만** 합니다. 차단 화면의 마크업을 본 적이 없으므로 그걸 추측해서 잡으려 하지 않았습니다(원칙 7). 대신 응답 본문이 500바이트 미만이면 차단 쪽, 정상 길이면 마크업 쪽으로 안내만 갈라줍니다.

#### 2026-09-08 실전 확인 (주딱 갤 `90_00_memory`, 명단 2명)

```
[18:06:36] 갤로그 점검: 2명을 확인합니다.
[18:06:39]   2명 확인 — 탈퇴 0명, 코드 확인 필요 0명, 정상 2명, 판단 불가 0명
[18:06:39]   2명의 글·댓글 수를 기록했습니다.
```

**경고 한 줄 없이 2명 다 읽혔습니다.** 그중 `capture6180`이 글 4,972 / 댓글 9,288이라, 쉼표 수정 전이었으면 반드시 `글·댓글 수를 읽지 못했습니다`가 떴을 계정입니다. 2-4절의 쉼표 문제가 실물에서 잡힌 것을 확인했습니다.

이때 안내 문구가 `1분쯤 걸립니다`라고 했는데 실제로는 3초였습니다. 인원이 적으면 초로 말하도록 고쳤습니다. 팝업 확인창은 더 어긋나 있었습니다. **입력칸의 상한값(기본 50)으로 계산해서**, 명단이 2명이어도 50명 기준으로 안내했습니다. 실제로 볼 인원으로 계산하게 고쳤습니다.

#### 아직 모르는 것

- **1.2초가 충분한지 모릅니다.** 안전한 간격의 실제 경계를 모릅니다. 또 막히면 더 늘리세요. **절대 줄이지 마세요.**
- 몇 요청부터 걸리는지, 시간당 상한인지 총량인지 모릅니다.
- 갤로그 점검 말고 다른 경로(명단 채우기 3000페이지)도 위험할 수 있습니다. 아직 사고는 없었습니다.
  **v1.6.6에서 정기 확인(`runCheck`)을 1.2초 + 난수로 올렸습니다.** 거기만 난수가 빠진 채
  400ms 고정이었고, 한 명당 요청 한 번이라 사고 때와 모양이 같았습니다. 게다가 사람이
  누르지 않아도 알람으로 돕니다. 명단 채우기는 간격에 난수가 걸려 있긴 하나 기준값이
  300~500ms 그대로입니다. 다만 그쪽은 한 요청에 30행씩 가져오므로 사람당 한 요청인
  두 경로와는 모양이 다릅니다. **다음에 손댈 만한 곳입니다.**

#### 파딱이 낸 대안 (아직 안 한 것)

v1.6.5는 1번과 2번, 4번의 일부를 했습니다. 3번은 손대지 않았습니다.

1. 조회를 느리게 하거나 간격을 난수로 → **했음**
2. 기록이 가장 오래된 유저부터 단계적으로 → **했음** (`gallogCountedAt` 기준 정렬)
3. 갤로그 기록 기능을 점검에서 분리해, 명단 추가할 때나 여유 있을 때 미리 조금씩 → **안 함.** 백그라운드에서 알람을 타고 하루 몇 명씩 도는 방식이 될 텐데, 그러면 완장이 모르는 사이에 요청이 나갑니다. 사고 직후에 넣을 기능은 아니라고 봤습니다.
4. 기록 시점이 설정 기간을 넘긴 대상만 점검 → **일부.** 12시간 규칙만 넣었습니다.

### 5-6. 주딱 갤 전체 기능 점검 — 2026-09-09 완료

`90_00_memory`에서 한도와 무관한 항목을 전부 돌렸습니다. **결과는 5-7절에 있습니다.**

**⚠ 옛 기준값에 속지 마세요.** 이 문서에는 한때 "2페이지에서 차단 이력 49행이 나오면 통과"라고 적혀 있었습니다. 그건 2026-09-06 값이고, 이틀간 시험 차단을 돌리면서 이력이 불어나 **2026-09-09에는 21페이지 602행**이었습니다. 하루에만 90행 넘게 쌓입니다. **차단 이력 행 수는 기준값으로 쓰지 마세요.** 대신 이런 것들을 보세요.

- `[경고]` 줄이 없는지 (행 수 대조 실패)
- `[안내]` 줄이 없는지 (앞 페이지와 같은 내용이라 멈춤 = `p` 파라미터 이상)
- 마지막 페이지에서 깔끔히 끝나는지

남은 절차는 이렇습니다. 0단계와 7단계는 **하루 차단 한도가 풀린 뒤에만** 됩니다.

0. 아무나 **1시간 차단**하고 손대지 않기. 식별코드 메모. (7단계에서 씀)
7. 한 시간 뒤 → `지금 확인` → 후보 탭 → `재차단 실행` → **관리 화면에서 눈으로 확인**

테스트용 계정 코드입니다.
`capture6180 chip3298 read7286 debate3002 leaf4517 apple8748 zzzz9999`
(`apple8748`은 살아있는 계정. **`zzzz9999`는 404용 가짜가 아니라 실제 탈퇴 계정입니다** — 2026-09-08 확인. 진짜 '없는 코드'를 보려면 `zqxjkw4821` 처럼 무작위 10자리를 쓰세요)

### 5-7. 검증 완료된 것

**⚠ 아래는 v1.7.3까지의 기록입니다. v1.7.4~v1.7.7은 실제 디시로 확인한 것이 하나도 없습니다 (5-9절).**

가짜 서버로만 확인한 것과 **실제 디시로 확인한 것**을 구분해 둡니다. 이 구분이 중요합니다. 이 프로젝트에서 난 사고는 대부분 "테스트는 통과하는데 실제로는 안 되는" 종류였습니다.

**실제 디시로 확인 (2026-09-08, 브라우저 자동화 + 확장 실행)**

- 차단 목록 마크업이 안 바뀜 — 확장이 쓰는 선택자 7개 전부 정상
- 한 페이지 30행, `p` 파라미터 동작
- 숨겨진 처리 시각(`처리 시간 : 14:10:08`) 추출
- 이력 30건에서 **최신 행만 보고** `candidate` 판정 (옛 해제됨 행에 안 속음)
- 자연 만료 → `candidate`, `manual` 오판 없음
- 하루 차단 한도 감지 → 중단 → 후보 유지 → **목록 대조로 실제 미전송 확인**
- 갤로그 세 갈래: 정상 16~20KB / 탈퇴 103B / 없는 코드 1130B
- 갤로그 글·댓글 수 (**천 단위 쉼표 포함**)
- 탈퇴/없는 코드 구분이 진짜로 되는지 — 무작위 10자리 ID 4개로 확인

**실제 디시로 확인 (2026-09-09, 주딱 갤 `90_00_memory`)**

- **차단 목록 21페이지 602행 순회.** `p` 파라미터가 21페이지까지 정상. 22페이지가 비어 깔끔히 종료(`[안내]` 없음), 행 수 대조 통과(`[경고]` 없음). **3페이지 이상 갤러리 확인이 여기서 끝났습니다**
- 날짜 기준 조기 종료: 21페이지 → 4페이지
- 페이지 상한 경고: `10페이지 상한에 걸렸습니다. 가장 오래된 행이 2026.09.07`
- 31일 필터 + 해제자 제외: 602행에서 31일 4명 추출 → 전원 해제 상태라 0명
- `이미 해제된 사람도 보기`: 껐을 때 0명 / 켰을 때 3명
- 사유 칩: 전부 해제 시 0명 + 안내 문구, 하나만 켜면 그것만
- 명단 담기: 3명 중 명단에 없는 1명만 추가
- **활동 점검 판정이 갈림**: `2명 중 0명` → (탈퇴 계정 추가) → `3명 중 1명`
- **갤로그 12시간 규칙**: 두 번째 누르면 `확인할 대상이 없습니다`, 요청 0번
- **부분 건너뛰기**: 새 1명만 조회하고 기존 2명은 건너뜀
- **탈퇴 판정 실물**: `zzzz9999` → 탈퇴 1명
- 삭제 화면 정렬: 탈퇴 붉게, 글 없음 회색, 갤로그 활동 첫 점검은 `-`
- **재차단 실전 성공 (09-09 0시 이후).** 두 명이 31일로 걸렸고 관리 화면 604·603번으로 확인. 사유 승계, `직접 차단` 경로, 후보 탭에서 빠짐, 재차단 이력 2건까지 (5-1절)
- **v1.6.9 화면**: 탭 4개, `명단` 탭의 세 갈래, 토글 동작 확인

**v1.7.x 검증 (2026-09-09, 주딱 갤)**

- **직접 입력 사유 재차단 성공.** 파일 불러오기(`실험2` 그대로 들어감) → 만료 → 후보(`실험2`) → `재차단 실행` → `디시 응답: 차단되었습니다` → 확인까지. **`avoid_reason=0`이 `user_codes` 경로에서도 먹습니다** (파딱 캡처는 `nos[]` 경로라 보내봐야 알 수 있었습니다)
- 만료 판정이 시각을 제대로 봄: 15:11 `이상 없음` → 15:22 `재차단 필요`
- `할 일` 탭 한 번에 불러오기: 처음이라 7일 전 날짜를 잡았고, 두 번째는 `lastScanAt` 이후만 훑음
- 채우기 기본 140페이지: 작은 갤에서 1페이지 읽고 멈춤 (헛돌지 않음)
- 담으면서 갤로그 미리 기록, 그리고 방금 기록한 사람을 건너뛰고 안 본 사람만 보기

**가짜 서버 / 실제 마크업 픽스처로만 확인**

- 목록 파싱 (실제 페이지 HTML + 검색 강조)
- 식별자 3종 구분, 닉에 괄호가 든 경우 포함
- 만료 시각 계산, 수동 해제 구분
- 명단 채우기 + 사유 필터 + 해제자 제외
- 여러 페이지 순회, 500자 분할
- 요청 간격 난수 (수치만. 서비스워커에서 실제로 흔들리는지는 미확인)
- 상태 칸 파싱 실패 경보, 판정 이어가기

**원본 파일을 읽어서 확인 (2026-09-09 추가)**

같은 실수가 두 번 난 자리들입니다. `test.mjs`가 소스를 직접 읽어 검사합니다.

- `background.js`에 난수를 안 거친 대기가 남아 있지 않은지
- 설정 화면의 `조회 한 명당 N초`가 `CHECK_DELAY_MS`와 같은지
- 설정 화면의 `기본값: N`이 코드의 `DEFAULTS`와 같은지
- `popup.html`에 `식별자`·`식별 코드` 표기가 남아 있지 않은지
- 모든 탭·갈래에 짝이 되는 화면이 있는지

### 5-8. 아직 확인 못 한 것

**파딱 없이 시간만 지나면 되는 것**

- **10월 10일 자연 만료** (5-1). 09-09 재차단으로 31일 시계가 켜졌습니다. 그날 만료 → 후보 재등장 → 재차단까지 보면 한 바퀴가 닫힙니다
- **갤로그 숫자 변동 비교.** 09-08에 첫 기록을 남겼지만 확장 데이터가 날아가 사라졌습니다(5-1절). **기준점을 다시 만들고, 12시간 뒤에 두 번째로 돌려야** 이 기능이 처음으로 뜻을 가집니다
- 하루 차단 한도가 몇 번인지. 세려면 차단을 반복해야 해서 권하지 않습니다

**해볼 수 있는데 아직 안 한 것**

- 서비스워커에서 요청 간격 난수가 실제로 먹는지. 예전에 "브라우저가 타이머를 눌러 측정이 안 됐다"고 적혀 있었는데 **잰 곳이 틀렸습니다.** 페이지가 아니라 `chrome://extensions` → 확장 카드의 `서비스 워커` 콘솔에서 재야 합니다. 요청이 0번 나갑니다
- 명단 파일 사유가 음란성으로 통일되던 문제(6절). 원인은 **옛 버전 파일에 사유 칸이 없어서**로 진단했지만 **실물 파일로 확인하지 못했습니다.** 파딱이 그 파일을 주기로 했습니다. 불러와서 확인창에 `N명은 파일에 사유가 없어` 문구가 뜨면 진단이 맞고, 안 뜨는데 사유가 통일되면 원인이 딴 데 있습니다
- 갤 내부 검색의 `댓글` 옵션이 정말 댓글 작성자로 못 찾는지
- ~~공지 필터~~ → **2026-09-09 확인 완료.** 프로그래밍 갤 1페이지(52행)를 `parseBoardList` 규칙에 그대로 물려서 검증했습니다. 공지 1건(2020년 글) 제외, 광고·설문 2건 제외, 글 49개를 읽었고 시각 못 읽은 행 0건. 공지 작성자는 `data-uid`가 빈 문자열이라 이중으로 걸러집니다

**파딱(또는 큰 갤) 없이는 못 하는 것**

- **1.2초 간격이 IP 차단을 막아주는지** (5-5). 증명하려면 대량 실행이 필요한데 그게 곧 사고라 **구조적으로 확인 불가**입니다
- **디시가 차단한 갤로그 화면** (5-3). 실물을 본 사람이 파딱뿐입니다
- 대형 갤러리에서 활동 점검 실제 소요 시간
- 명단이 수백~수만 명일 때의 실제 동작

**⚠ 흉내내지 마세요.** 부하를 재보겠다고 가짜 코드 수백 개를 명단에 넣으면 안 됩니다. 명단에 들어간 사람은 `nextCheckAt`이 0이라 **다음 정기 확인 때 전원이 조회 대상**이 됩니다. 300명을 넣으면 크롬이 알아서 300번 요청을 보냅니다. 파딱 IP가 막혔을 때와 같은 조건이고, 이번엔 주딱 회선입니다.

### 5-9. v1.7.4 ~ v1.7.8 은 실제 디시로 확인한 것이 거의 없습니다 ⚠

**검사만 통과한 상태입니다.** 5-7절의 구분(가짜 서버 vs 실제 디시)을 기억하세요.

**다만 v1.7.8이 고친 것들은 파딱의 실사용 기록에서 나온 것이라, 증상 쪽은 실물로 확인됐습니다.** 확인 안 된 것은 고친 결과입니다. 다음 확인에서 봐야 할 것은 딱 하나입니다 — **진행 기록이 `[300/300]` 까지 가는가.** 예전엔 50쯤에서 멈췄습니다. 이 프로젝트에서 난 사고는 대부분 "테스트는 통과하는데 실제로는 안 되는" 종류였습니다. 지금 이 네 버전이 정확히 그 위치에 있습니다.

확인해야 할 순서입니다. 위에서부터 값어치가 큽니다.

| 무엇 | 어떻게 | 누가 |
|---|---|---|
| **재차단이 여전히 되는가** | 주딱 갤에서 1시간 차단 → 만료 → `재차단 실행` → **관리 화면에서 눈으로** | 주딱 혼자 가능 |
| **재확인을 켜도 되는가** | 설정 → 고급 → `실행 전에 후보를 다시 확인` 켜고 위를 한 번 더 | 주딱 혼자 가능 |
| 예상 시간이 맞는가 | 300명쯤 도는 갤에서 안내 문구와 실제 시간 대조 | 파딱 |
| 갤로그 기록 동봉 | 한쪽이 명단 내보내기 → 다른 쪽이 불러오기 → **조회 0번**으로 기준점이 생기는지 | 완장 둘 |
| 재확인이 실제로 걸러내는가 | 한쪽이 손으로 차단 → 다른 쪽이 `재차단 실행` → `1명은 그 사이에 이미 차단됐습니다` 가 뜨는지 | 완장 둘 |

| **조회가 끝까지 도는가** | `지금 확인` → 진행 기록이 `[300/300]`까지 가는지. 중간에 멈추면 ①이 안 고쳐진 것 | 주딱·파딱 누구든 |
| **옛 명단 되메우기** | 채우기 다시 → 담기 → `이미 명단에 있던 N명의 만료 예정 시각을 채웠습니다` 가 뜨는지 | 파딱 (4577명) |

**아래 두 줄은 완장이 둘 이상이어야 뜻이 있습니다.** 주딱 갤 혼자서는 겹칠 일이 없어 검증이 안 됩니다. 파딱이 돌아오기 전까지는 `실행 전에 후보를 다시 확인`을 **켜지 말라고 안내하는 게 안전합니다.** 켜서 얻는 게 없고, v1.7.6에서 이 경로가 터진 전례가 있습니다.

**v1.7.6을 쓰고 있는 사람이 있으면 먼저 v1.7.7로 올리게 하세요.** 1.7.6에서 저 토글을 켜면 재차단이 통째로 안 됩니다(0절).

---

## 6. 알려진 한계

- 크롬이 켜져 있어야 동작합니다. 꺼져 있던 시간의 예정 확인은 소급하지 않습니다.
- 자동 업데이트가 없습니다. 새 버전은 다시 받아 덮어쓰고 확장 새로고침해야 합니다.
- 완장이 만료 직전(10분 이내)에 푼 경우는 자연 만료와 구분하지 못합니다.
- **글 목록에 댓글이 안 나옵니다.** 댓글로만 활동하는 사람이 '글 없음'으로 잡힙니다.
- 비공개 갤로그와 디시가 차단한 갤로그를 구분하지 못합니다. 둘 다 `alive`입니다.
- **갤로그 점검이 느립니다.** 50명에 1분입니다. IP 차단을 피하려고 일부러 그렇게 뒀습니다(5-5절). 명단이 수천 명이면 한 바퀴 도는 데 며칠 걸립니다.
- 명단 채우기 쪽 요청 간격 기준값은 아직 300~500ms입니다(난수는 걸립니다). 사람당 한 요청인 갤로그 점검과 정기 확인만 1.2초로 올렸습니다.
- 한 사람의 이력이 30행을 넘으면 검색 결과 1페이지만 봅니다. 목록이 최신순이라 판정에는 문제없습니다.
- 웹스토어에 올리지 않아 크롬이 주기적으로 "개발자 모드 확장을 사용 중지할까요"를 물어봅니다. (등록비 5달러, 의뢰인이 원치 않아 깃허브 배포)
- **옛 버전으로 내보낸 명단 파일은 사유가 전부 `음란성`이 됩니다.** 그 파일에 사유 칸 자체가 없어서 복원할 방법이 없습니다. 지금은 내보낼 때 사유가 들어가고, 사유 없는 파일을 불러오면 몇 명이 그렇게 되는지 경고합니다. 값 자체는 여전히 음란성이라 명단 탭에서 직접 고쳐야 합니다.
- ~~릴리즈 zip 안 `설치법.txt` 파일명 깨짐~~ → **v1.7.7에서 해결.** 압축을 파이썬 `zipfile` 로 만들면 비-ASCII 파일명에 UTF-8 플래그가 자동으로 붙습니다. **깃허브에서 저장소를 zip 으로 받으면 여전히 `#Uc124#Uce58#Ubc95.txt` 로 나옵니다** — 그건 깃허브가 만드는 압축이라 우리가 손댈 수 없습니다. 릴리즈에 첨부하는 zip 만 우리가 만드는 것이고, 완장이 받는 건 그쪽입니다.
- **재확인 기능은 완장이 둘 이상일 때만 뜻이 있습니다.** 혼자면 조회만 늘고 얻는 게 없어 기본이 꺼짐입니다 (5-9절).
- **첫 갤로그 점검에서는 아무 판정도 안 나옵니다.** 비교할 이전 숫자가 없기 때문입니다. 명단 파일로 남의 기록을 받아오면 그 사람들은 건너뜁니다 (v1.7.5).

---

## 7. 유지보수 실무

### 테스트

```
node test.mjs               # 265개 검사
TZ=Asia/Seoul node test.mjs # 시간대 전용 검사 2개 추가 (267개)
node test-popup.mjs         # 팝업 화면 88개 검사
node test-bg.mjs            # 재차단 실행 경로 50개 검사   ← v1.7.7
```

**세 파일 다 1초 안에 끝납니다.** `test.mjs` 가 예전엔 80초였는데, 요청 간격을 실제로 자고 있었기 때문입니다. 지금은 `globalThis.setTimeout` 을 0ms 로 감싸 **순서는 그대로 두고 시간만** 없앱니다. 오래 걸리는 검사는 안 돌리게 되고, 안 돌리는 검사는 없는 것과 같습니다.

**개수는 기준값으로 쓰지 마세요.** 검사를 늘리면 바뀝니다. `0 failed` 인지만 보세요.

**픽스처는 실제 관리 화면에서 복사한 마크업입니다. 지어낸 마크업으로 테스트하지 마세요.** v1.5.2의 옛 테스트가 그랬는데, 실제 구조(`blockstate txtbtn`, `<p></p>` 껍데기, 셀 안 `<script>`)와 달라서 통과해도 아무것도 보장하지 못했습니다.

**픽스처는 `fixtures.mjs` 한 곳에 있습니다** (v1.7.7). `test.mjs` 와 `test-bg.mjs` 가 같이 씁니다. 양쪽에 따로 적어두면 한쪽만 고쳤을 때 어긋나는데 검사는 둘 다 통과합니다.

**검사 대상 함수를 테스트 파일에 옮겨 적지 마세요.** 그러면 복사본만 검사하게 되어 실제 코드가 바뀌어도 통과합니다. 옛 테스트가 15행 기준 페이지 계산식을 옮겨 적어놔서, dc.js가 30행으로 바뀐 뒤에도 계속 통과했습니다.

**팝업 화면 검사는 `test-popup.mjs`에 있습니다.** 설치할 것이 없습니다.
`popup.html`에서 id를 긁어 최소한의 DOM을 만들고, `chrome.storage`/`chrome.runtime`을
흉내낸 뒤 `popup.js` 원본을 `vm`으로 돌립니다. 체크박스를 실제로 눌러가며 확인합니다.

두 가지가 이 방식의 값어치입니다. 첫째, **popup.html에 없는 id를 부르면 그 자리에서
터집니다.** 오타난 id가 조용히 넘어가지 않습니다. 둘째, 표의 체크박스를 popup.js가
만든 HTML에서 직접 긁어오므로 클래스 이름이 어긋나면 잡힙니다.

원본을 읽어서 돌리는 것이 핵심입니다. 함수를 여기 옮겨 적으면 복사본만 검사하게 됩니다.

**재차단 실행 경로 검사는 `test-bg.mjs`에 있습니다** (v1.7.7). 설치할 것이 없습니다.
`test-popup.mjs`와 같은 방식으로 `background.js` 원본을 `vm`으로 돌리고, `fetch`와
`chrome`을 가짜로 바꿔 끼웁니다.

두 가지가 이 방식의 값어치입니다.

**첫째, 가짜 디시가 POST를 받으면 실제로 상태를 바꿉니다.** 응답만 흉내내면
"차단되었습니다라고 답하고 아무 일도 안 한다"는 실제 디시의 성질(원칙 1번)을
검사가 못 보게 됩니다. 지금은 차단 요청이 나가면 목록에도 반영되고, 확장이
그 목록을 다시 읽어 검증하는 것까지 한 바퀴가 실제로 돕니다.

**둘째, 요청이 몇 번 나갔는지를 셉니다.** 이 프로젝트에서는 "몇 번 나갔나"가
곧 IP 차단 위험입니다. `한도를 넘으면 조회 0회` 같은 것은 그렇게만 확인됩니다.

주의할 점 두 개입니다.

- **`dc.js`는 진짜 모듈로 import되므로 sandbox가 아니라 전역의 `fetch`·`chrome`을 씁니다.**
  `globalThis.fetch`를 안 바꾸면 진짜 요청이 나가서 전부 `fetch failed`가 됩니다.
- 실행 경로에는 1.2초 간격과 검증 전 1초 대기가 박혀 있습니다. 그대로 두면 검사
  하나에 몇 초씩 걸립니다. `globalThis.setTimeout`을 0ms로 감싸 **순서는 그대로 두고
  시간만** 없앴습니다.

**새 검사는 일부러 되돌려서 진짜 실패하는지 확인한 뒤에 넣으세요** (원칙 8번).
v1.7.7의 44개는 다섯 가지 되돌림으로 각각 15·1·4·1·2개가 실패하는 것을 보고
넣었습니다. 통과하는 것만 보고 안심하면 아무것도 안 지켜집니다.

### 디시가 마크업을 바꿔서 깨졌을 때

기록 창에 `표에 N행이 있는데 M행만 읽었습니다` 경고와 알림이 뜹니다.
관리 화면에서 F12 → 해당 `<table>` 우클릭 → Copy → Copy outerHTML로 실제 HTML을 받아 정규식을 맞추세요. 받은 HTML을 test.mjs 픽스처로 넣고 돌리면 어느 칸이 깨졌는지 바로 나옵니다.

### 명단 채우기가 일부만 불러올 때

기록 창의 `N페이지에서 차단 이력 M행을 읽었습니다`를 보세요. `다음 페이지가 앞 페이지와 같은 내용이라 멈췄습니다`가 함께 뜨면 `p` 파라미터가 바뀌었을 가능성이 큽니다.

### 통신이 바뀌었을 때

F12 → Network → **`로그 보관` 체크** → 지우기 → 차단 실행.
`로그 보관`을 켜지 않으면 차단 후 페이지가 새로고침되어 요청이 사라집니다.

### 브라우저로 디시를 직접 확인할 때

2026-09-08에 이 방법으로 버그 4개를 찾았습니다. 코드만 읽어서는 안 나왔을 것들입니다.

**확장 팝업은 자동화가 안 됩니다.** 툴바 아이콘은 브라우저 UI라 페이지가 아니고, `chrome-extension://` 주소로 탭을 여는 것도 도구가 막습니다. **버튼은 사람이 눌러야 합니다.** 대신 디시 쪽 대조는 자동화가 되고, 그게 손이 더 많이 가는 쪽입니다.

관리 화면에서 콘솔로 이렇게 훑으면 빠릅니다. 같은 출처라 `fetch`가 그대로 됩니다.

```js
const r = await fetch('/mgallery/management/block?id=90_00_memory&s=&t=u'
  + '&s_type=search_user_id&s_keyword=capture6180', {credentials:'include'});
const d = new DOMParser().parseFromString(await r.text(), 'text/html');
[...d.querySelectorAll('table.minor_block_list tbody tr')].map(tr => ({
  번호: tr.querySelector('.blocknum')?.textContent.trim(),
  기간: tr.querySelector('.blocktime')?.textContent.trim(),
  상태: tr.querySelector('td[class*=blockstate]')?.textContent.replace(/\s+/g,' ').trim(),
}));
```

**주의 두 가지입니다.**

- 갤로그(`gallog.dcinside.com`)는 **다른 출처**라 관리 화면에서 `fetch`하면 CORS에 막힙니다. 갤로그 탭으로 옮겨간 뒤에 하세요.
- **시각을 문자열로 비교하지 마세요.** `"9:30:00" > "17:00:00"` 이 참이 됩니다. 실제로 이것 때문에 한 번 잘못 읽었습니다.

### 버전 올리기

1. `manifest.json`의 `version` 수정
2. 깃허브 저장소에 바뀐 파일 업로드 (`Add file` → `Upload files` → 끌어다 놓기 → `Commit changes`)
3. `Releases` → `Draft a new release` → 태그 `vX.Y.Z` → 배포용 zip 첨부 → `Publish release`
4. README 변경 이력도 채우기

**배포용 zip에 넣을 파일** (README/PRIVACY/STORE/검사 파일/handover는 제외)
```
manifest.json background.js dc.js popup.html popup.css popup.js
icon16.png icon48.png icon128.png 설치법.txt
```

**zip은 파이썬으로 만드세요.** 그래야 `설치법.txt`에 UTF-8 플래그가 붙어 맥/리눅스에서 파일명이 안 깨집니다(6절).

```python
import zipfile
files = ["manifest.json","background.js","dc.js","popup.html","popup.css","popup.js",
         "icon16.png","icon48.png","icon128.png","설치법.txt"]
with zipfile.ZipFile("dcblock-vX.Y.Z.zip","w",zipfile.ZIP_DEFLATED) as z:
    for f in files: z.write(f, f)
```

**다운로드 수 확인** — 릴리즈 화면에는 안 나옵니다.
`https://api.github.com/repos/onlyFaker-lab/dcinside-autoblock/releases` 를 브라우저로 열고 `download_count`를 찾으세요. 방문자 수는 저장소 `Insights` → `Traffic`(본인 로그인 필요, 14일치).

---

## 8. 변경 이력 요약

| 버전 | 핵심 |
|---|---|
| 1.5.1 | storage에 Date 객체를 담아 `Invalid Date`가 되던 문제 수정 |
| 1.5.2 | busy 잠금 영구 고착, 설정 저장 시 항목 유실, 검증 상한 제거 |
| 1.5.3 | (릴리즈 없음) 파싱 실패 감지, 사유 공백 흡수, busy 갱신 |
| 1.5.4 | **명단 채우기가 1페이지만 불러오던 문제 수정.** 페이저 링크 대신 `p` 직접 증가 |
| 1.5.5 | 명단 채우기 사유 필터 |
| 1.5.6 | 공용 헬퍼 일원화, test.mjs를 실제 마크업으로 재작성 |
| 1.5.7 | 하루 차단 한도 감지, 후보 복사/파일 공유, 여러 명 한 번에 |
| 1.5.8 | `이 날짜까지` 옵션, 페이지 상한 3000, 메모리 상한 |
| **1.6.0** | **명단 정리 탭 (활동 점검 + 갤로그 점검 + 확인 후 삭제)** |
| 1.6.1 | 날짜 우선 훑기, 명단 백업에 메모 포함, 죽어 있던 머리글 체크박스 |
| **1.6.2** | **갤로그 글·댓글 수로 비활성 판정** (파딱 요청), 하루 한도 실제 문구 반영 |
| 1.6.3 | (릴리즈 없음) 탈퇴 판정 1차 수정 — 여전히 안 잡혔음 |
| 1.6.4 | **탈퇴 판정 해결.** 실제 응답은 스크립트 한 줄이라 그 안의 경로를 본다 |
| **1.6.5** | **갤로그 천 단위 쉼표 수정**, IP 차단 대응(간격 1.2초+난수, 기본 50명, 연속 실패 중단), 명단 채우기 해제자 제외, 상태 칸 파싱 실패 경보, 판정 이어가기, 한도 때 성공처럼 보이던 문구 수정, 예상 시간 수정 |
| **1.6.6** | **정기 확인 간격 400ms 고정 → 1.2초 + 난수.** 1.6.5가 갤로그만 고치고 여기를 빠뜨렸다. 조회 예상 시간 3배 과대 수정, 고정 간격 대기를 잡는 검사 추가 |
| **1.6.7** | **화면 재구성. 탭 6개 → 4개.** `명단 정리`→`빼기`, `이력`/`기록`→`재차단 이력`/`작업 기록`, 식별코드 표기 통일, 설정 기본/고급 분리, 빈 명단 안내. 설정의 `조회 한 명당 0.4초` 문구가 1.6.6과 어긋난 채 남아 있던 것 수정 |
| 1.6.8 | 토글 스위치, 설정 카드. **색을 남색으로 바꿨는데 추측이라 틀렸다 (1.6.9에서 되돌림)** |
| **1.6.9** | DCRefresher Reborn 실물 캡처 보고 맞춤. 중성 회색 복귀, 켜진 토글 초록, 카드 12px, **설정마다 기본값 표시**(코드와 어긋나면 검사 실패). **이 버전으로 재차단 실전 성공** |
| **1.7.0** | **직접 입력 사유 재차단.** `avoid_reason=0` + `avoid_reason_txt`. 명단 파일을 불러올 때 모르는 사유를 음란성으로 바꾸지 않음 (파딱 갤 4533명이 전원 직접 입력 사유였다) |
| 1.7.1 | 갤로그 점검이 기준점 안 된 사람 건너뜀(4533 → 1113명), 담으면서 갤로그 미리 기록, `밀린 31일 차단 불러오기`, 로그인 풀려도 만료 예정 인원 알림, 채우기 기본 140페이지 |
| **1.7.2** | **로그아웃 감지.** 디시는 안내 문구가 아니라 87바이트 스크립트 한 줄을 준다. `/로그인/` 검사가 절대 안 걸렸고 `매니저 권한을 확인하세요`라는 틀린 진단이 떴다 (2-9절) |
| 1.7.3 | 명단에 넣을 때도 직접 입력 사유, 명단 행 `수정`, `중지만 보기`, 사유로 검색 |
| **1.7.4** | **로그인 풀림 때 0명으로 나오던 것 수정.** 고르는 쪽은 `nextCheckAt === 0`을 대상으로 치는데 세는 쪽만 걸러냈다(파딱 갤 4575명 전원). 예상 시간에 조회 시간(`FETCH_SECS`) 추가, 며칠에 걸쳐 한 바퀴 돈다는 안내 |
| **1.7.5** | **명단 파일에 갤로그 기록 동봉** (`mergeGallog`). 받는 쪽은 조회 0번으로 기준점을 얻는다. 이미 명단에 있는 사람도 기록은 받아옴. `popup.js`가 `dc.js`를 import하기 시작 |
| 1.7.6 | `실행 전에 후보를 다시 확인` 토글 (기본 꺼짐) |
| **1.7.7** | **1.7.6의 그 토글이 켜는 순간 터지던 것 수정** — `analyzeCode`에 `entry` 누락. 한도 검사를 재확인 앞으로, 재확인 중 잠금, 표 못 읽으면 중단, 판정 바뀐 사람 따로 알림. **`test-bg.mjs` 신설**(44개), `fixtures.mjs` 분리, 배포 zip UTF-8 파일명 |
| **1.7.8** | **확장이 영원히 진행되지 않던 것 수정.** MV3 워커가 30초 문턱에 걸려 50명에서 죽고, 결과는 끝에 한꺼번에 저장하는 구조라 통째로 사라지고, 다음 차례에 또 같은 자리에서 죽었다. keepAlive(20초) + 25명마다 중간 저장. 명단 채우기가 만료 예정 시각을 담고 옛 명단도 되메움(4577명이 전원 매번 조회 대상이었다). 잠금 만료 30분→3분, 로그아웃인데 계획부터 안내하던 것, 검사 80초→0.4초 |

---

## 9. 디시 용어

| 용어 | 뜻 |
|---|---|
| 주딱 | 갤러리 매니저. 개설자이며 운영 전권 |
| 파딱 | 부매니저. 삭제·차단 권한 위임받음 |
| 31차 | 31일 차단. 완장이 걸 수 있는 최장 기간 |
| 분탕 | 갤러리 분위기를 망치는 악성 유저 |
| 고닉 / 반고닉 / 유동닉 | 고정 닉네임 / 식별코드 있는 비회원 / 완전 비회원 |
| 신문고 | 차단 이의 제기 창구 |
| 갤로그 | 디시 계정의 개인 페이지 |

---

## 10. 다음 작업자에게

의뢰인은 개발 경험이 있지만 깃허브나 크롬 확장은 처음입니다. **파일을 줄 때는 "이걸 어디에 어떻게 올려라"까지 구체적으로 적어주세요.** 압축파일로 뭉뚱그려 주면 헷갈려 합니다.

이 프로젝트의 문제 대부분은 **"됐다고 나오는데 실제로는 안 된"** 종류였습니다. 확신이 서지 않으면 성공했다고 말하지 말고, 관리 화면에서 직접 확인해달라고 요청하세요. 그게 지금까지 가장 잘 통한 방법입니다.

그리고 **의뢰인이 낸 아이디어를 무시하지 마세요.** 활동 점검을 갤로그로 하려다 막혔을 때, 의뢰인이 "갤스코프처럼 갤에서 검색하면 되지 않냐"고 했고 그게 정답이었습니다. 글 목록에 `data-uid`가 있다는 걸 그 덕에 찾았고, 요청 수가 명단 크기와 무관해졌으며, 비공개 갤로그 문제도 통째로 사라졌습니다.

**검사를 늘릴 때는 "다시 그 실수를 할 수 있는가"를 기준으로 하세요.** 이 프로젝트에서 실제로 두 번 난 실수가 셋 있습니다. ① 상수는 고쳤는데 코드가 리터럴을 그대로 씀 ② 코드는 고쳤는데 화면 설명이 옛날 값을 말함 ③ 같은 것을 여러 이름으로 부름. 지금은 셋 다 `test.mjs`가 원본 파일을 읽어서 잡습니다. 새로 만든 검사는 **일부러 되돌려서 진짜 실패하는지** 확인한 뒤에 넣으세요. 안 그러면 통과하는 것만 보고 안심하게 됩니다.

**그런데 원본 파일을 읽는 검사에는 함정이 있습니다.** 그건 **어긋남**을 잡는 도구지 **동작**을 확인하는 도구가 아닙니다. v1.7.6이 그걸 헷갈렸습니다. 새 기능에 대해 "소스에 이 조건문이 있는가", "이 중단 문구가 있는가"를 정규식으로 확인하고 내보냈는데, 켜자마자 첫 후보에서 죽었습니다. 검사 257개가 전부 초록이었습니다. **기능을 넣었으면 그 기능을 돌려보세요.** `test-bg.mjs`가 그 자리를 메우려고 만든 것이고, 같은 방식으로 다른 경로(명단 채우기, 갤로그 점검)에도 붙일 수 있습니다. 아직 안 붙어 있습니다.

**버전을 올리면 이 문서도 같이 고치세요.** v1.7.3 이후 네 버전 동안 한 번도 안 고쳐져서, 2026-09-12에 몰아서 정리했습니다. 그 사이 이 문서를 믿고 읽은 사람은 검사 개수도 파일 목록도 틀린 것을 봤을 것입니다. 5-6절에 "옛 기준값에 속지 마세요"라고 적어둔 것이 이 문서 자신에게도 해당됩니다.

**의뢰인은 IP 차단 사고 뒤로 이 프로젝트를 계속해야 할지 고민했습니다.** 갈라서 봐주세요. 사고는 갤로그 점검 하나에서 났고 그건 부가 기능입니다. 본체인 재차단은 명단에 있는 사람만, 만료된 사람만 조회해서 하루 수십 번 수준입니다. 명단 정리를 아예 안 써도 재차단은 돕니다. 다만 **"고쳤다"고 말하지는 마세요.** 지금 할 수 있는 말은 "같은 조건을 다시 만들지 않는다"까지입니다.
