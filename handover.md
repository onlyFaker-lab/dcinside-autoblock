# 디시 자동 갱신차단 — 인수인계서

> 이 문서 하나만 읽고 유지보수를 이어받을 수 있도록 작성했습니다.
> 2026-09-06 기준. 크롬 확장 v1.5.6 배포 완료 상태입니다.

---

## 1. 무엇을 만들었나

디시인사이드 마이너 갤러리의 차단 기간 최대치는 **31일**입니다. 그래서 악성 유저가
31일 뒤에 돌아와 같은 짓을 반복하고, 완장은 그때마다 수동으로 다시 차단해야 합니다.

이 프로그램은 **등록해둔 대상의 차단이 풀렸는지 하루 두 번 확인하고, 풀렸으면
31일로 재차단**합니다. 자동으로 차단하지 않고 후보만 보여준 뒤 사용자가 버튼을
눌러야 실행됩니다.

두 가지 구현이 있습니다.

| | 파이썬 GUI | 크롬 확장 (주력) |
|---|---|---|
| 동작 | 크롬이 꺼져 있어도 돎 | 크롬이 켜져 있어야 돎 |
| 설치 | 파이썬 + 셀레니움 필요 | 폴더 로드만 하면 끝 |
| 로그인 | secrets.json에 아이디/비번 저장 | 브라우저 세션 그대로 사용 (저장 안 함) |
| 상태 | 동작 확인됨, 유지보수 중단 | **배포 중, 이쪽을 주로 씁니다** |

의뢰인은 `90-00년대 추억 갤러리`(id: `90_00_memory`)의 매니저입니다.
실제 관리 화면에서 테스트가 가능합니다.

배포처: `https://github.com/onlyFaker-lab/dcinside-autoblock`

---

## 2. 디시 관리 화면에 대해 알아낸 사실

**이 절이 이 문서에서 가장 중요합니다.** 전부 실제 화면과 통신을 확인해서 얻은
것이고, 대부분은 문서에 없는 내용입니다.

### 2-1. 차단 목록 페이지

```
https://gall.dcinside.com/mgallery/management/block?id=<갤러리ID>
```

검색 파라미터 (실제 확인됨):

```
&s=            상태 필터 (A=전체 B=차단중 R=해제됨, 빈 값=전체)
&s_type=       search_nickname / search_user_id / search_user_ip
               / search_subject / search_comment / search_reason
&s_keyword=    검색어
```

**상태 필터(`s=`)는 믿으면 안 됩니다.** `s=B`(차단 중)로 걸렀는데 해제됨 행이
섞여 나오는 걸 확인했습니다. 그래서 코드는 필터를 쓰지 않고 상태를 직접 읽습니다.

### 2-2. 페이지 넘김 (2026-09-06 확인, v1.5.4)

**페이지 파라미터는 `page`가 아니라 `p`입니다.** `t=u`는 이용자 차단 탭이고
이미지 차단은 `t=i`입니다. 실제 페이저가 내보내는 주소:

```
/mgallery/management/block?id=90_00_memory&s=&t=u&p=2
```

**한 페이지는 30행입니다.**

**`bottom_paging_box` 클래스가 페이지에 두 개 있습니다.** 하나는 화면 위쪽
헤더의 즐겨찾기 레이어 안에 있고(`<em>1</em>` 뿐, 링크 없음), 진짜 목록
페이저는 아래쪽에 있습니다. `html.match()`는 첫 번째만 돌려주므로 위쪽 것을
잡으면 링크가 하나도 안 나옵니다. **v1.5.3까지 명단 채우기가 1페이지만
불러오던 원인이 정확히 이것이었습니다.**

지금은 페이저 링크를 아예 보지 않습니다. `p` 번호를 직접 올려가며 읽고,
행이 없거나 앞 페이지와 같은 내용이면 멈춥니다. 화면 구조 의존이 사라졌습니다.
디시가 범위 밖 페이지에 1페이지를 되돌려주는 경우도 행 단위로 걸러집니다.

### 2-3. 목록 테이블 구조

```
table.minor_block_list > tbody > tr
  td.gall_chk             체크박스
  td.blocknum[data-num]   내부 차단 ID (행마다 고유)
  td.blocknik             닉네임 + (식별코드 또는 IP)
  td.blockcontent         게시글/댓글 링크 또는 '직접 차단'
  td.blockreason          사유
  td.blocktime            차단 기간 ('31일')
  td.blockday             span.block_date(날짜)
                          + 숨겨진 p.block_time('처리 시간 : 15:06:30')
                          + 숨겨진 p.block_conduct('처리자 : ...')
  td.blockstate.txtbtn    '해제됨'  또는  '차단 중' + 해제 버튼
```

실제 마크업의 함정들입니다.

- **`td.blockstate`의 진짜 클래스는 `blockstate txtbtn`입니다.** 정규식으로
  잡을 때 `class="blockstate"` 완전일치를 쓰면 못 잡습니다. 못 잡으면
  `released`가 전부 false가 되어 **후보가 한 명도 안 잡히는데 경고도 안 뜹니다.**
  그래서 상태 칸을 읽지 못한 행은 따로 표시해서(`stateUnknown`) 경고에 셉니다.
- 차단 중인 행의 상태 칸 텍스트는 해제 버튼까지 합쳐 `차단 중 해제`가 됩니다.
  판정은 `해제됨` 포함 여부로 하므로 오판하지 않습니다.
  (버튼: `set_avoid(this, 'R', '<차단번호>', 0)`)
- **처리 시각이 숨겨진 툴팁 안에 있습니다.** 화면에는 날짜만 보이지만 DOM에는
  초 단위 시각이 있습니다. 이 덕분에 만료 시각을 계산할 수 있고, 그게 이 프로그램의
  핵심 장치 두 개(수동 해제 구분, 대량 명단 스케줄링)를 가능하게 합니다.
  셀레니움에서는 `display:none`이라 `.text`가 빈 문자열이므로 `textContent`를 써야 합니다.
- **`td.blocknik` 안에 jQuery 템플릿 `<script>`가 들어 있습니다.** 그 안에
  "중복 차단으로 기존 차단은 해제되었습니다" 같은 문구가 있어서, 제거하지 않으면
  파싱이 오염됩니다. 파싱 전에 스크립트를 걷어내야 합니다.
- **닉과 코드가 빈 `<p></p>` 껍데기에 싸여 있습니다.**
  `<p></p><p>ㅇㅇ</p><p></p>` 다음 줄에 `<p></p><p>(chip3298)</p><p></p>` 식입니다.
- **닉네임 자체에 괄호가 들어갈 수 있습니다.** `송재원(새우젓눈깔) (119.65.*.*)`
  처럼요. 그래서 **마지막** 괄호 그룹을 식별자로 봐야 합니다.
- **검색 결과에서는 검색어가 태그로 감싸져 강조됩니다.** `(lea<span>f451</span>7)`처럼
  단어 중간이 쪼개질 수 있어서, 태그를 **공백 없이** 제거한 뒤 텍스트에서 뽑아야 합니다.
  (공백으로 치환하면 코드가 `lea f451 7`로 깨집니다)
- 체크박스 `input[name="chk_avoid_user[]"]`의 **id가 모든 행에서 `list_chk`로 중복**입니다.
  `label[for=list_chk]`를 클릭하면 항상 첫 행만 토글됩니다.

### 2-4. 차단 실행 API (실제로 동작하는 유일한 경로)

```
POST https://gall.dcinside.com/ajax/managements_ajax/user_code_avoid
Content-Type: application/x-www-form-urlencoded; charset=UTF-8
X-Requested-With: XMLHttpRequest

ci_t=<토큰>&gallery_id=<갤러리ID>&_GALLTYPE_=M
&user_codes=<식별코드, 줄바꿈으로 여러 명>
&avoid_hour=744&avoid_reason=1&avoid_reason_txt=
```

- `ci_t` = **`ci_c` 쿠키 값 그대로**입니다. (`gall.dcinside.com` 도메인)
  HTML 안에는 없습니다. 확장은 `chrome.cookies.get`으로 읽습니다.
- `avoid_hour`: 1 / 6 / 24 / 168 / 336 / **744(31일)**
- `avoid_reason`: 1음란성 2광고 3욕설 4도배 5혐오콘텐츠 6저작권침해 7명예훼손 0직접입력
- `user_codes`는 **500자 제한**이 있습니다. 넘으면 나눠 보내야 합니다.
- **화면 표기와 코드 값의 공백이 다를 수 있습니다.** 목록에 `혐오콘텐츠`로 나오는데
  차단 팝업의 라벨은 `혐오 콘텐츠`입니다. 목록에서 읽은 사유를 그대로 들고 가면
  나중에 `알 수 없는 사유`로 그룹 전체가 실패합니다. 공백을 무시하고 맞춘 뒤
  아는 값으로 되돌려 씁니다.

### 2-5. 동작하지 않는 것들 (중요)

**목록 체크박스 + 하단 `차단` 버튼은 작동하지 않습니다.**
확인 버튼을 누르면 "차단되었습니다" 알림이 뜨지만 목록에 행이 생기지 않고
관리 기록에도 남지 않습니다. **브라우저에서 손으로 눌러도 동일**하므로
자동화 문제가 아니라 디시 쪽 문제로 보입니다. (2026-09-05 확인)

이것 때문에 초기 구현이 통째로 헛돌았습니다. 나중에 디시가 고치면 이 경로로
`+ IP`나 IP 대역까지 원형 그대로 재차단할 수 있게 되니, 가끔 확인해볼 가치는 있습니다.

**IP 차단은 지원하지 않습니다.** 디시는 매니저에게도 IP를 앞 2옥텟까지만
보여줍니다(`183.103.*.*`). 직접 차단 팝업은 식별코드만 받으므로 IP를 넣을 수도
없습니다. 설령 가능해도 통신사 대역을 통째로 막는 셈이라 무고한 이용자만 막히고
정작 상대는 IP를 바꾸면 그만입니다. **의도적으로 제외한 것이며 되살리지 마세요.**

**재차단하면 항상 코드 단독 차단이 됩니다.** 원래 `+ IP`로 걸려 있던 사람도
마찬가지입니다. 직접 차단 팝업이 식별코드만 받기 때문입니다.

### 2-6. 그 밖의 성질

- 해제된 목록은 **30일만 보관**됩니다. 그 뒤에는 추적할 방법이 없습니다.
- 재차단해도 **옛 해제됨 행이 30일간 남습니다.** 그래서 "해제됨 행이 있으면 재차단"
  같은 단순 규칙을 쓰면 이미 막아둔 사람을 매번 다시 막게 됩니다.
- 중복 차단하면 기존 차단이 해제되고 새로 걸립니다. 남은 기간이 초기화됩니다.
- 로그인 직후 **비밀번호 변경 캠페인 페이지**(`sign.dcinside.com/campaign/password_change`)로
  튕기는 경우가 있습니다. 파이썬 버전은 이걸 자동으로 넘깁니다.

---

## 3. 확장 구조

```
manifest.json     MV3, 권한: storage unlimitedStorage alarms cookies notifications
                  host_permissions: https://gall.dcinside.com/*
dc.js             디시 통신 + HTML 파싱 + 판정 로직 (핵심)
background.js     서비스워커. 스케줄, 조회, 재차단, 명단 채우기
popup.html/css/js 화면 (탭 5개: 후보 / 명단 / 명단 채우기 / 이력 / 설정)
test.mjs           브라우저 없이 돌리는 검사. `node test.mjs`
icon16/48/128.png
README.md PRIVACY.md STORE.md 설치법.txt .gitignore
```

**MV3 서비스워커에는 DOMParser가 없습니다.** 그래서 HTML 파싱을 정규식으로 합니다.
뽑을 게 표 몇 칸뿐이라 충분하지만, 디시가 마크업을 바꾸면 여기가 먼저 깨집니다.

### 3-1. dc.js 주요 함수

| 함수 | 역할 |
|---|---|
| `parseBlockList(html)` | 목록 테이블 → 행 배열 (+ `expectedRows`/`missedRows`) |
| `analyzeCode(rows, code, entry, now)` | 판정. `candidate`/`manual`/`active`/`none` + `nextCheckAt` |
| `fetchRowsForCode(gid, code)` | 식별코드로 검색 조회 |
| `crawlList(gid, maxPages, onPage)` | `p` 번호를 올려가며 목록 순회 |
| `blockCodes(gid, codes, reason, hours, onProgress)` | 재차단 + 검증 |
| `collectByDuration(gid, '31일', pages)` | 명단 채우기용 수집 |
| `chunkCodes(codes)` | 500자 단위 분할 |
| `expiresAt(row)` / `isManualRelease(row)` | 만료 시각 계산 / 수동 해제 판정 |

`parseBlockList`가 돌려주는 배열에는 `expectedRows`(표에 있던 행 수)와
`missedRows`(그중 못 읽은 수)가 붙어 옵니다. 빈 결과 자체는 정상일 수 있으므로
(이력 없는 코드 검색) 이 대조 없이는 파싱이 깨진 걸 알 수 없습니다.

### 3-2. 판정 규칙 (analyzeCode)

**가장 최근 행 하나만 보고 판단합니다.** 옛 행은 재차단 흔적이나 중복 차단으로
일찍 해제된 흔적이라 오판의 원인이 됩니다.

```
차단 중인 행이 있음        → active   (다음 확인 = 그 행의 만료 시각 + 1분)
최신 행이 만료 전에 해제됨 → manual   (완장이 직접 푼 것. 재차단 안 함. 7일 뒤 재확인)
최신 행이 만료돼서 해제됨  → candidate (재차단 후보)
행이 아예 없음             → none     (1일 뒤 재확인, 60일 넘으면 자동 중지)
```

`manual` 판정은 `처리시각 + 차단기간`을 계산해 **예정보다 10분 이상 일찍** 풀렸으면
사람이 푼 것으로 봅니다. 신문고 민원으로 풀어준 사람을 12시간 만에 다시 막는
사고를 방지하기 위한 장치입니다.

### 3-3. 대량 명단 대응 (v1.4.0~)

명단이 수만 명이 될 수 있습니다(경기날 분탕이 수백 명씩 오는 갤러리 대상).
한 명당 조회 한 번이므로 그대로 두면 감당이 안 됩니다.

**만료 예정 시각을 기억해두고 그때까지 조회하지 않습니다.** 그래서 조회량이
명단 크기가 아니라 그날 만료되는 인원에 비례합니다.

| 명단 | 하루 조회 |
|---|---|
| 1,000명 | 약 30회 |
| 10,000명 | 약 320회 |
| 100,000명 | 약 3,200회 |

- 한 번에 조회할 인원 상한: `maxChecksPerRun` (기본 300)
- 예약이 어긋날 수 있으므로 매 확인마다 오래 확인 안 한 사람을 몇 명씩
  섞어 봅니다: `sweepPerRun` (기본 20)
- 즉시 전원 재확인: 명단 탭의 `전체 다시 확인` 버튼
- 60일간 기록 없으면 자동 중지 (해제 목록이 30일만 보관되므로 추적 불가)

### 3-5. 명단 채우기의 사유 필터 (v1.5.5)

파딱 요청으로 넣었습니다. 목록을 한 번 훑은 뒤 `state.imports` 안에 실제로
존재하는 사유만 칩으로 만들어 보여주고, 고른 사유에 해당하는 사람만 화면에
남깁니다. 서버에 다시 묻지 않으므로 사유를 바꿔가며 봐도 요청이 늘지 않습니다.

디시 검색에 `s_type=search_reason` 이 있어서 서버 쪽으로 거를 수도 있지만,
그러면 기간 필터와 조합이 안 되고 요청이 사유 수만큼 늘어납니다. 한 번 훑고
화면에서 거르는 편이 단순하고 정확합니다.

주의할 점 두 가지입니다.

- **칩을 전부 해제한 상태를 '전체'로 되돌리지 마세요.** 처음에 그렇게 만들었다가
  테스트에서 걸렸습니다. 사용자는 아무것도 안 고른 줄 아는데 화면에는 전원이
  떠서, 그대로 `전체 선택 → 추가`를 누르면 엉뚱한 사람이 명단에 들어갑니다.
  지금은 0명을 보여주고 안내 문구를 띄웁니다.
- **칩 상태를 화면에서 읽지 마세요.** `render()`가 체크박스를 새로 만들기 때문에,
  화면을 훑으면 방금 교체된 요소를 보게 되는 경우가 생깁니다. 지금 상태
  (`scanReasons`)에서 하나를 더하거나 빼는 방식으로 계산합니다.

### 3-4. 저장 데이터 (chrome.storage.local)

```
settings   { galleryId, checkTimes[], maxPerRun, maxChecksPerRun,
             sweepPerRun, autoApply, notify }
watchlist  [{ kind:'code', value, reason, memo, enabled,
              nextCheckAt, lastVerifiedAt, lastSeen, missingSince }]
candidates [] 재차단 후보
manual     [] 완장이 직접 풀어준 것으로 보이는 대상
imports    [] 명단 채우기 결과
history    [] 재차단 이력
logs       [] 기록 창 내용 (최근 200개)
status     { text, busy, busySince }
```

**chrome.storage는 Date 객체를 담지 못합니다.** 빈 값으로 바뀌어 `Invalid Date`가
됩니다. 시각은 반드시 숫자(`getTime()`)로 저장하세요. v1.5.1에서 이 문제로 한 번
고쳤습니다.

**`busy` 잠금에는 `busySince`가 같이 붙습니다.** MV3 서비스워커는 작업 도중에도
종료되는데, 그러면 `busy`가 true인 채 남아 이후 알람이 전부 되돌아가고 팝업
버튼도 굳습니다. 확장이 조용히 영구 정지합니다. 30분이 지난 잠금은 무시하고,
브라우저 재시작 시 풀며, 긴 조회 중에는 25명마다 `busySince`를 갱신합니다.

---

## 4. 반드시 지켜야 할 원칙

이 프로젝트에서 실제로 사고가 났거나 날 뻔했던 것들입니다.

**1. 서버 응답을 믿지 마세요.**
디시는 아무 일도 하지 않고 "차단되었습니다"라고 응답합니다. 실제로 이것 때문에
두 번이나 "성공했다"고 잘못 보고했습니다. 차단 후 **목록을 다시 읽어 31일 행이
실제로 생긴 것만** 성공으로 기록해야 합니다.

**2. 조용한 실패를 만들지 마세요.**
이 프로젝트의 버그는 거의 전부 이 형태였습니다.

- 검색 강조 태그 때문에 식별자를 못 읽어 아무도 재차단되지 않음 (에러 없음)
- 페이저를 엉뚱한 걸 잡아 1페이지만 불러옴 (에러 없음, v1.5.4에서 수정)
- 표를 한 행도 못 읽으면 결과가 빈 배열이라 '이력 없음'과 구분 안 됨

완장은 잘 돌아간다고 믿는데 실제로는 아무도 안 막히는 상태가 최악입니다.
**뭔가 못 읽었으면 반드시 눈에 보이게 남기세요.** 행 수를 세서 대조하고,
페이지를 넘겼는지 기록에 남기고, 상태를 못 읽은 행도 실패로 세십시오.

**3. 자동으로 차단하지 마세요.**
후보만 보여주고 사용자가 버튼을 눌러야 실행됩니다. `autoApply` 옵션이 있지만
기본은 꺼져 있고, 켜라고 권하지 마세요.

**4. 명단에 등록된 대상만 갱신하세요.**
"만료된 차단을 전부 자동 갱신"은 절대 금지입니다.

**5. 한도를 두세요.**
후보가 `maxPerRun`(기본 100)을 넘으면 실행하지 않고 멈춥니다. 파싱이 어긋나
멀쩡한 사람까지 무더기로 차단되는 사고를 막는 장치입니다.

**6. 서버에 부담을 주지 마세요.**
조회는 하루 1~2회. 요청 사이 400ms(목록 순회는 300ms) 간격. 짧은 주기 폴링 금지.

---

## 5. 검증 방법

이 프로젝트는 **실제 화면 확인으로만 검증됐습니다.** 의뢰인이 매번 관리 화면을
눈으로 대조해준 덕에 문제를 여럿 잡았습니다. 코드가 "됐다"고 하는 걸 믿지 말고
목록에 행이 실제로 생겼는지 확인하게 하세요.

**오프라인 테스트가 가능한 부분**: 파싱, 판정, 분할, 스케줄 계산, 페이지 순회.
node로 실제 페이지 HTML과 가짜 fetch를 만들어 검증할 수 있습니다. 실제로 그렇게 했습니다.
관리 화면에서 표를 통째로 복사해오면(F12 → `<table>` 우클릭 → Copy outerHTML)
그대로 픽스처로 쓸 수 있습니다.

저장소의 `test.mjs`를 `node test.mjs`로 돌리면 66개 검사가 실행됩니다.
픽스처는 실제 관리 화면에서 복사한 마크업입니다. **지어낸 마크업으로 테스트하지
마세요.** v1.5.2의 옛 테스트가 그랬는데, 실제 구조(`blockstate txtbtn`,
`<p></p>` 껍데기, 셀 안 `<script>`)와 달라서 통과해도 아무것도 보장하지
못했습니다. 그리고 검사 대상 함수를 테스트 파일에 옮겨 적지 마세요. 그러면
복사본만 검사하게 되어 실제 코드가 바뀌어도 통과합니다.

팝업 화면 로직도 `jsdom`으로 검증할 수 있습니다. `popup.html`을 읽어 DOM을 만들고
`chrome.storage` / `chrome.runtime`만 흉내낸 뒤 `popup.js`를 `eval`하면, 실제로
체크박스를 눌러가며 동작을 확인할 수 있습니다. 사유 필터의 버그 두 개를 이 방법으로
잡았습니다.

**검증 완료된 것**
- 목록 파싱 (실제 페이지 HTML + 검색 강조 6종)
- 식별자 3종 구분 (code / code+IP / IP), 닉에 괄호가 든 경우 포함
- 숨겨진 처리 시각 추출
- 자연 만료 감지 → 31일 재차단 (1시간 차단으로 실제 확인)
- 수동 해제 구분
- 명단 채우기 (사유 필터 포함, jsdom으로 화면 조작까지 검증)
- 500자 분할
- **여러 페이지 순회 (2026-09-06, 실제 49행 2페이지 갤러리에서 확인)**

**아직 확인 못 한 것**
- **실제 31일 만료.** 1시간 차단으로 원리는 확인했지만 31일은 2026년 10월 초에
  처음 겪게 됩니다.
- 명단이 수백~수만 명일 때의 실제 동작.
- 목록이 3페이지 이상인 갤러리. (2페이지까지는 실제 확인)

---

## 6. 알려진 한계

- 크롬이 켜져 있어야 동작합니다. 꺼져 있던 시간의 예정 확인은 소급하지 않습니다.
- 자동 업데이트가 없습니다. 새 버전은 다시 받아 덮어쓰고 확장 새로고침해야 합니다.
- 완장이 만료 직전(10분 이내)에 푼 경우는 자연 만료와 구분하지 못합니다.
- 명단이 만 명이면 점검(sweep)만으로 전체가 한 바퀴 도는 데 250일 걸립니다.
  어긋난 기록을 즉시 바로잡으려면 `전체 다시 확인`을 눌러야 합니다.
- 한 사람의 이력이 30행을 넘으면 검색 결과 1페이지만 봅니다. 목록이 최신순이라
  판정에는 문제가 없지만, 옛 이력은 보이지 않습니다.
- 웹스토어에 올리지 않아 크롬이 주기적으로 "개발자 모드 확장을 사용 중지할까요"를
  물어봅니다. (등록비 5달러, 의뢰인이 원치 않아 깃허브 배포를 선택)

---

## 7. 유지보수 시 자주 필요할 것들

**디시가 마크업을 바꿔서 파싱이 깨졌을 때**
기록 창에 `표에 N행이 있는데 M행만 읽었습니다` 경고와 알림이 뜹니다.
관리 화면에서 F12 → 해당 `<table>` 우클릭 → Copy → Copy outerHTML로 실제 HTML을
받아서 `dc.js`의 정규식을 맞추세요. 받은 HTML을 node 픽스처로 넣고
`parseBlockList`를 돌려보면 어느 칸이 깨졌는지 바로 나옵니다.

**명단 채우기가 일부만 불러올 때**
기록 창의 `N페이지에서 차단 이력 M행을 읽었습니다` 줄을 보세요.
M이 실제 이력보다 적으면 페이지 넘김이 안 되는 것입니다.
`다음 페이지가 앞 페이지와 같은 내용이라 멈췄습니다`가 함께 뜨면
`p` 파라미터가 바뀌었을 가능성이 큽니다. 관리 화면에서 2페이지를 눌러
주소창의 주소를 확인하세요.

**통신이 바뀌었을 때**
F12 → Network → `로그 보관` 체크 → 지우기 → 차단 실행.
**`로그 보관`을 켜지 않으면 요청이 사라집니다.** 차단 후 페이지가 통째로
새로고침되기 때문입니다. 이것 때문에 요청을 찾는 데 한참 걸렸습니다.

**버전 올리기**
`manifest.json`의 `version`을 올리고, 깃허브에 파일을 덮어쓰고, 릴리즈를 만들어
배포용 zip을 첨부합니다. README의 변경 이력도 같이 채우세요.

**배포용 zip에 넣을 파일**
```
manifest.json background.js dc.js popup.html popup.css popup.js
icon16.png icon48.png icon128.png 설치법.txt
```
(README/PRIVACY/STORE는 뺍니다)

---

## 8. 디시 용어

| 용어 | 뜻 |
|---|---|
| 주딱 | 갤러리 매니저. 개설자이며 운영 전권 |
| 파딱 | 부매니저. 삭제·차단 권한 위임받음 |
| 31차 | 31일 차단. 완장이 걸 수 있는 최장 기간 |
| 분탕 | 갤러리 분위기를 망치는 악성 유저 |
| 고닉 / 유동닉 | 고정 닉네임(계정) / 비회원 임시 닉네임 |
| 신문고 | 차단 이의 제기 창구 |

---

## 9. 다음 작업자에게

의뢰인은 개발 경험이 있지만 깃허브나 크롬 확장은 처음입니다.
터미널 명령이나 클릭 순서는 구체적으로 짚어주는 편이 좋습니다.

그리고 이 프로젝트의 문제 대부분은 **"됐다고 나오는데 실제로는 안 된"** 종류였습니다.
확신이 서지 않으면 성공했다고 말하지 말고, 관리 화면에서 직접 확인해달라고
요청하세요. 그게 지금까지 가장 잘 통한 방법입니다.

특히 **화면 구조를 추측하지 마세요.** v1.5.2에서 `blockstate` 클래스에 접미사가
붙는다고 추측해 정규식을 고쳤는데, 실제로는 두 번째 클래스(`blockstate txtbtn`)
였습니다. v1.5.3에서는 페이지 파라미터를 `page`로 짐작했는데 실제로는 `p`였고,
그대로 냈으면 페이지 순회가 여전히 안 됐을 겁니다. 둘 다 실제 HTML을 받아보고서야
드러났습니다. **표 하나 복사해달라고 부탁하는 게 몇 시간을 아낍니다.**
