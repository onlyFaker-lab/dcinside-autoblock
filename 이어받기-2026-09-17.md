# 이어받기 메모 — 2026-09-17

> 다른 AI에게 작업을 넘기기 위한 문서입니다.
> **먼저 `handover.md` 를 읽으세요.** 그게 본 문서이고, 이건 "지금 어디까지 왔고
> 다음에 뭘 해야 하는지"만 적은 짧은 메모입니다.

---

## 0. 한 줄 요약

디시인사이드 마이너 갤러리 완장용 크롬 확장입니다. 31일 차단이 만료된 악성 유저를
찾아 완장이 버튼을 눌러 재차단합니다. **자동으로 차단하거나 지우지 않습니다.**

현재 **v1.7.15**. 검사 312 / 127 / 110 전부 통과.

저장소: `https://github.com/onlyFaker-lab/dcinside-autoblock`

---

## 1. 지난번 메모의 세 가지는 끝났습니다

깃허브 업로드, 릴리즈, 파딱 답장 전부 완료. 저장소에 v1.7.14 파일 20개가
올라가 있는 것을 확인했습니다.

**⚠ 릴리즈 태그 표기를 확인하세요.** v1.7.11 이 `v.1.7.11` 로 점이 하나 더
들어가 있습니다. 새로 만들 때 그 화면을 보고 따라 쓰면 같은 오타가 납니다.

---

## 2. v1.7.15 에서 한 것

### ① v1.7.14 의 치명적 버그 수정

`background.js` 중괄호가 어긋나, 글·댓글 수를 못 읽는 계정을 하나라도 만나면
**갤로그 점검이 통째로 죽었습니다.** `Cannot read properties of null`.

하필 그 조건이 **디시가 IP를 막아 빈 응답을 줄 때**입니다. 2026-09-08 사고와
같은 상황이라, 가장 필요할 때 죽는 모양이었습니다. 연속 실패 중단 장치도 같이
무력화돼 있었습니다.

**검사 97개가 전부 통과하고 있었습니다.** 가짜 디시가 언제나 멀쩡한 화면만
돌려줬기 때문입니다. 지금은 `test-bg.mjs` 가 못 읽는 화면도 줍니다([24]).

### ② 갤로그 구역별 최신 날짜

파딱 갱차 시트 3,740행을 대조해보니, 파딱이 실제로 쓴 기준은 글·댓글 '수'가
아니라 **마지막 활동 '날짜'** 였습니다. 자세한 것은 `handover.md` 0절.

```
gallogPostAt / gallogCommentAt / gallogGuestAt
gallogLastAt        셋 중 가장 최근
gallogPostsOpen / gallogCommentsOpen    공개 여부
```

요청은 안 늡니다. 전부 이미 받아오던 화면 안에 있습니다.
**숫자 변동과 달리 첫 바퀴부터 판정이 나옵니다.**

---

## 3. 사람들

| 누구 | 역할 |
|---|---|
| **주딱** | 갤러리 매니저. 이 확장을 의뢰한 사람 |
| **파딱** | 부매니저. 실사용 피드백. 로그를 캡처해서 보내줍니다 |
| **다른 파딱** | 또 다른 부매니저. 갱신차단을 직접 하고 명단을 구글시트로 관리 |

**파딱 갤 명단이 4,700명이 넘습니다.** 이 규모에서만 드러나는 문제가 많았습니다.

---

## 4. 다음 작업 — 빼기 탭 자동 판정

**재료는 다 모았습니다. 판정은 아직 안 넣었습니다.**

순서가 이렇습니다.

```
1. 파딱이 갤로그 점검을 300~500명씩 끊어 돌려 기록을 쌓는다
   (이때 IP가 막히는지도 같이 본다)
2. 빼기 탭에서 실제 숫자를 보고 기준을 정한다
3. 그 기준으로 자동 판정을 넣는다
```

**1번이 끝나기 전에 3번을 하지 마세요.**

시트 대조로 나온 참고값입니다.

| 기준 | 결과 |
|---|---|
| 마지막 활동이 한 달 이내면 활성 | 2,024건 중 98.3% 일치 |
| 방문자 ≤50 AND 글+댓 ≤50 | 521명 중 508명이 X (오판 13) |
| 다만 X의 18%는 숫자 그물에 안 걸림 | 숫자는 큰데 오래 쉰 계정 |

**숫자로는 절대 못 잡는 것이 있습니다.** 댓글 36,362개인데 `광고충` 이라고
해제한 계정이 있습니다. 자동 판정을 넣더라도 **자동 삭제로 만들면 안 됩니다.**

---

## 5. 아직 모르는 것

| 항목 | 상황 |
|---|---|
| **1.2초 간격이 충분한가** | 164명은 무사. **9월 8일 IP 차단 사고가 300명이었으므로 그 선이 기준.** 파딱이 300~500명씩 끊어 시험하기로 함 |
| `MANUAL_MARGIN_MS`(10분) 경계값 | 확인된 사례는 예정보다 30일 이른 해제라 10분이든 1시간이든 같은 결과. 기록의 `예정보다 N 이릅니다` 가 분 단위로 나오면 그때 판단 |
| 확장의 갤로그 조회가 방문자 수를 올리는가 | 올린다고 보고 `gallogSeenByUs` 로 보정 중 |
| **10월 10일 자연 만료** | 주딱 갤 `capture6180` `chip3298`. 그날 만료 → 후보 재등장 → 재차단까지 보면 한 바퀴가 닫힙니다. **그때까지 두 사람을 손대지 마세요** |

---

## 6. 일하는 방식 — 이것만은 지켜주세요

`handover.md` 4절에 원칙 열 가지가 있습니다. 전부 실제 사고에서 나온 것입니다.

### 화면 구조를 추측하지 마세요

상태 칸 클래스, 페이지 파라미터, 천 단위 쉼표, 방문자 수 클래스 뒤의 공백,
그리고 이번의 **게시글 구역만 클래스 접미사가 없는 것**까지 전부 실물을
받아보고서야 드러났습니다. **소스 한 장 달라고 부탁하는 게 몇 시간을 아낍니다.**

### 검사가 통과하는 것과 기능이 도는 것은 다릅니다

v1.7.6 은 검사 257개를 통과하면서 켜는 순간 터졌습니다.
v1.7.14 는 검사 97개를 통과하면서 갤로그 점검이 죽는 상태로 나갔습니다.
**두 번째는 "돌려보는 검사"가 있었는데도 났습니다** — 돌려보는 상황이
한쪽(멀쩡한 응답)뿐이었기 때문입니다.

**기능을 넣었으면 잘 되는 경우와 안 되는 경우를 둘 다 돌려보세요.**

### 새 검사는 일부러 되돌려서 실패를 확인한 뒤에 넣으세요

v1.7.15 의 새 검사는 전부 되돌림 확인을 거쳤습니다.

```bash
node test.mjs         # 312개. TZ=Asia/Seoul 로 돌리면 314개
node test-popup.mjs   # 127개
node test-bg.mjs      # 110개
```

세 파일 다 1초 안에 끝납니다. 설치할 것 없습니다.

### 요청 간격은 늘리기만 하세요

2026-09-08 에 실제로 IP가 막혔습니다. **완장 계정이 아니라 회선이 막혀서 그 집에서
디시 자체가 안 열립니다.** 줄이자는 생각이 들면 `handover.md` 5-5절을 먼저 읽으세요.

---

## 7. 완장에게 파일을 줄 때

**이 문장을 반드시 같이 적으세요.**

> 원래 확장 폴더에 덮어쓰고 `chrome://extensions` 에서 새로고침(↻)만 누르세요.
> 새 폴더에 풀어서 다시 추가하면 크롬이 다른 확장으로 보고 명단이 통째로 날아갑니다.

파딱 명단이 4,700명입니다. 한 번 날아가면 되돌릴 수 없습니다.
