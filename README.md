# 디시 자동 갱신차단 (크롬 확장)

차단이 만료된 악성 유저를 감지해 31일 재차단합니다.
마이너 갤러리 매니저(주딱) · 부매니저(파딱)용.

파이썬 버전과 하는 일은 같지만, 브라우저에 이미 로그인된 세션을 쓰기 때문에
아이디와 비밀번호를 어디에도 저장하지 않습니다.

> 크롬을 처음 다뤄보신다면 `설치법.txt` 를 보세요. 그림 없이 순서대로만 적어뒀습니다.

## 받기

오른쪽 위 초록색 `Code` 버튼 → `Download ZIP` 으로 받은 뒤 압축을 푸세요.

## 설치

1. 크롬 주소창에 `chrome://extensions` 입력
2. 오른쪽 위 **개발자 모드** 켜기
3. 왼쪽 위 **압축해제된 확장 프로그램을 로드합니다** 클릭
4. 이 폴더 선택

주소창 오른쪽 퍼즐 아이콘에서 이 확장을 고정해두면 편합니다.

## 처음 설정

확장 아이콘을 누르고 **설정** 탭에서 갤러리 ID를 입력한 뒤 저장하세요.
갤러리 주소 `gall.dcinside.com/mgallery/board/lists/?id=여기` 의 `여기` 부분입니다.

그 다음 **영구 관리 명단** 탭에서 재차단할 식별코드를 추가합니다.
차단 목록 화면 괄호 안에 보이는 값입니다. (`ㅇㅇ (abcd1234)` → `abcd1234`)

## 동작

설정한 시각(기본 09:30, 21:30)에 명단에 있는 사람을 하나씩 조회합니다.
차단이 풀린 사람이 있으면 **재차단 후보** 탭에 띄우고 멈춥니다.
`재차단 실행`을 눌러야 실제로 차단됩니다.

**크롬이 켜져 있어야 동작합니다.** 파이썬 버전과 다른 점입니다.
크롬이 꺼져 있던 시간의 예정 확인은 소급 실행하지 않습니다.

## 알아둘 것

**IP는 자동 갱신하지 않습니다.** 디시는 완장에게도 IP 앞 두 자리까지만 보여줍니다.
그 상태로 차단하면 통신사 대역을 통째로 막는 셈인데, 정작 상대는 IP를 바꾸면 그만이고
무고한 갤러만 막힙니다. 명단에 IP를 넣으려 하면 막습니다.

**재차단하면 항상 코드 단독 차단이 됩니다.** 원래 `+ IP`로 걸려 있던 사람도 마찬가지입니다.
디시의 직접 차단 기능이 식별코드만 받기 때문입니다.

**차단 결과를 검증합니다.** 서버가 성공이라고 응답해도 믿지 않고,
목록을 다시 읽어 31일 차단이 실제로 걸린 것만 성공으로 기록합니다.

**완장이 직접 풀어준 사람은 재차단하지 않습니다.** 신문고 민원 등으로 차단을
풀어준 경우, 기간이 남았는데 해제된 상태가 됩니다. 자연 만료와 구분해서
재차단 대신 따로 알려주고, 명단에서 뺄 수 있게 합니다.

## 명단 채우기

분탕이 많은 갤러리에서 식별코드를 하나씩 옮겨적는 건 무리입니다.
`명단 채우기` 탭에서 `31일 차단 불러오기` 를 누르면 차단 목록을 훑어
31일 차단을 걸었던 사람을 모아옵니다. 체크해서 한 번에 명단에 담을 수 있습니다.

이미 명단에 있는 사람과 IP 차단은 빼고 보여줍니다.
재차단 사유는 원래 걸었던 사유를 그대로 씁니다.

**어디까지 훑을지는 날짜로 정하는 편이 낫습니다.** `이 날짜까지` 에 날짜를
넣으면 그보다 오래된 차단이 나오는 순간 멈춥니다. 페이지 수를 감으로 찍을
필요가 없고, 필요 이상으로 훑지도 않습니다.

날짜를 넣으면 페이지 입력란은 흐려집니다. 날짜가 기준이고, 페이지 수는
안전장치(3000페이지, 90,000행)로만 남습니다. 날짜에 닿기 전에 그 상한에
걸리면 기록에 "가장 오래된 행이 언제였는지"가 남습니다.

한 페이지가 30행이고 요청 간격이 0.3초(1000페이지 초과 시 0.5초)라,
1000페이지면 대략 10~15분 걸립니다.

불러온 목록 위에 **사유 칩**이 나옵니다. 사유를 골라 그 사유로 차단했던 사람만
추릴 수 있습니다. 예를 들어 `광고`만 켜두면 광고로 차단했던 사람만 보이고,
`보이는 사람 전체 선택`을 누르면 그 사람들만 담깁니다.
`전체` 를 누르면 필터가 풀립니다.

날짜를 비워두면 페이지 수가 기준이 됩니다. 기본은 최근 10페이지입니다.

## 명단이 많을 때

명단이 수만 명이 되어도 견디도록 만들었습니다.

**만료가 임박한 사람만 조회합니다.** 31일 차단 중인 사람은 그동안 상태가 바뀌지
않으므로 만료 예정 시각까지 아예 건드리지 않습니다. 목록에 숨어 있는 처리 시각을
읽어 만료 시점을 계산해두고, 그때가 되면 확인합니다.

그래서 실제 조회량은 명단 크기가 아니라 그날 만료되는 인원에 비례합니다.

| 명단 | 하루 조회 |
|---|---|
| 1,000명 | 약 30회 |
| 10,000명 | 약 320회 |
| 100,000명 | 약 3,200회 |

한 번에 조회할 인원에 상한(기본 300명)이 있어 몰려도 나눠서 처리합니다.
설정 탭에서 `한 번에 조회할 최대 인원` 으로 조절할 수 있습니다.

**기록이 어긋날 수 있습니다.** 만료 예정 시각을 미리 계산해두는 방식이라,
그 전에 완장이 손으로 차단을 풀면 다음 만료 때까지 모르고 지나갑니다.
그래서 매 확인마다 오래 확인하지 않은 사람을 몇 명씩 섞어서 봅니다.
(설정 탭의 `매번 섞어서 볼 점검 인원`, 기본 20명)
바로 반영하고 싶으면 명단 탭의 `전체 다시 확인` 을 누르세요.

**오래 안 보이는 사람은 자동으로 쉽니다.** 해제된 목록은 30일만 보관되므로,
기록이 사라진 뒤에는 조회해도 알 수 있는 게 없습니다. 60일간 기록이 없으면
자동으로 중지 처리하고 알려줍니다. 지워지지는 않으니 필요하면 다시 켜면 됩니다.

**재차단은 묶어서 보냅니다.** 디시 입력 제한(500자)에 맞춰 자동으로 나누므로
인원이 많아도 누락되지 않습니다.

명단 탭에는 검색창이 있고 한 번에 200명씩 보여줍니다.

## 안전장치

- 후보를 찾아도 자동으로 차단하지 않음. 사용자가 버튼을 눌러야 실행
- 명단에 등록된 대상만 갱신. 만료된 차단을 전부 자동 갱신하지 않음
- 이미 차단 중인 대상은 후보에서 제외 (중복 차단 방지)
- 한 번에 100건 초과면 실행하지 않고 중단 (설정에서 조절)
- 조회는 하루 1~2회. 짧은 주기로 서버를 긁지 않음
- 재차단 이력을 남겨 사후 추적 가능

## 권한을 왜 받나

| 권한 | 쓰임 |
|---|---|
| `gall.dcinside.com` | 차단 목록 조회와 차단 실행 |
| `cookies` | 디시가 요구하는 인증 토큰(`ci_c`)을 읽기 위해 |
| `storage` | 명단·설정·이력 저장 (전부 내 컴퓨터 안에만) |
| `alarms` | 정해진 시각에 확인 |
| `notifications` | 후보를 찾았을 때 알림 |

외부로 데이터를 보내지 않습니다. 디시 외의 사이트에는 접근하지 않습니다.

## 파일

```
manifest.json   확장 설정
background.js   정기 확인, 재차단 실행
dc.js           디시 조회·차단 API, HTML 파싱
popup.html/css/js   화면
fixtures.mjs    검사들이 같이 쓰는 실제 관리 화면 마크업
test.mjs        파싱·판정 검사 (node test.mjs)
test-popup.mjs  화면 동작 검사 (node test-popup.mjs)
test-bg.mjs     재차단 실행 경로 검사 (node test-bg.mjs)
```

## 변경 이력

### 1.7.8
- **조회가 늘 중간에 끊기던 것을 고쳤습니다.** MV3 서비스워커는 30초 동안
  chrome API 호출이 없으면 종료되는데, `fetch`와 `setTimeout`은 그 타이머를
  되살리지 못합니다. 진행 기록이 25명마다라 33초 간격이었고, 300명 조회가
  50명쯤에서 죽었습니다. 이제 20초마다 chrome API를 불러 워커를 살려 둡니다.
- **죽어도 그때까지 조회한 결과가 남습니다.** 예전에는 마지막에 한꺼번에
  저장해서, 워커가 죽으면 50명분이 통째로 사라졌습니다. 그 사람들은 다음
  차례에 똑같이 뽑혀 똑같은 자리에서 또 죽었습니다. **하루 두 번 며칠을 돌아도
  한 명도 진행되지 않았습니다.** 이제 25명마다 중간 저장합니다.
- **'지금 확인'이 굳어 있는 시간이 30분에서 3분으로 줄었습니다.** 죽은 작업이
  남긴 잠금을 30분이나 붙들고 있었습니다.
- **명단 채우기로 담을 때 만료 예정 시각을 같이 저장합니다.** 예전에는 비워
  뒀는데, 빈 값은 '한 번도 못 봤다'는 뜻이라 **만료가 한 달 남은 사람까지 매번
  조회 대상**이 됐습니다. 4577명을 8일에 걸쳐 도는데 실제로 볼 값어치가 있는
  사람은 몇 명뿐이었습니다. 이제 만료될 때만 봅니다.
- **이미 명단에 있던 사람의 빈 만료 시각도 채워 줍니다.** 채우기를 한 번 더
  돌리고 담기를 누르면 됩니다. 옛 버전으로 만든 명단을 고치는 길입니다.
- **로그아웃 상태에서 `9분쯤 걸립니다`를 먼저 안내하던 것을 고쳤습니다.**
  계획 안내가 먼저 나오고 그 다음 줄에 `[오류] 로그인이 풀린 것 같습니다`가
  와서, 완장은 9분짜리 작업이 시작된 줄 알고 기다렸습니다. 이제 첫 조회가
  성공한 뒤에 안내합니다.
- 검사가 80초에서 0.4초로 줄었습니다(실제 대기 시간을 그대로 자고 있었습니다).

### 1.7.7
- **`실행 전에 후보를 다시 확인`을 켜면 재차단이 통째로 터지던 것을 고쳤습니다.**
  1.7.6에서 이 기능을 켜면, 후보가 아직 풀려 있는 경우 —  즉 재차단해야 하는
  바로 그 경우 — 에 `Cannot read properties of undefined (reading 'reason')`
  로 죽고 한 명도 차단되지 않았습니다. 판정 함수에 넘겨야 할 값을 빠뜨린
  것이었습니다. 반대로 후보가 전원 이미 차단돼 있으면 멀쩡히 지나가서,
  하필 아무 일도 안 해도 되는 날에만 정상으로 보였습니다.
- **후보가 한도를 넘으면 재확인 조회조차 하지 않습니다.** 순서가 반대였습니다.
  후보 300명에 한도 100이면, 300번을 다 조회한 뒤에 `한도 초과로 중단`이
  떴습니다. 한 명도 못 막으면서 사람당 한 요청씩 300번이 나가는 것이라
  IP 차단 사고와 모양이 같았습니다.
- **재확인이 도는 동안 잠급니다.** 안 잠가서, 재확인 중에 알람이 정기 확인을
  띄우면 두 경로가 겹쳐 돌았습니다. 요청이 두 배로 나가면 IP 차단을 피하려고
  넣은 1.2초 간격이 의미가 없어집니다. 긴 재확인 도중 잠금이 굳은 것으로
  오인되지 않게 갱신도 합니다.
- **재확인 중에 표를 제대로 못 읽으면 멈춥니다.** 상태 칸을 못 읽으면 해제된
  사람이 전부 '차단 중'으로 보이는데, 그 판정으로 사람을 빼면 조용히 재차단을
  건너뛰게 됩니다.
- **판정이 바뀐 사람을 '이미 차단됨'으로 뭉개지 않습니다.** 목록에서 사라진
  것(해제 기록 30일 보관 만료)과 다른 완장이 이미 차단한 것은 원인이 다릅니다.
  누가 왜 빠졌는지 코드까지 기록에 적습니다.
- **`test-bg.mjs`를 새로 만들었습니다.** 위 버그들이 검사 257개를 전부 통과한
  이유는, 재차단 실행 경로에 동작 검사가 하나도 없었기 때문입니다. 소스에
  글자가 있는지 보는 검사는 그 글자가 실제로 도는지 말해주지 않습니다.
  이제 가짜 디시를 세워 두고 `background.js` 원본을 그대로 돌려서, 요청이 몇 번
  나갔는지까지 셉니다. 새 검사 44개는 전부 **일부러 되돌려서 실패하는 것을
  확인한 뒤에** 넣었습니다.
- 픽스처를 `fixtures.mjs`로 빼서 검사들이 같은 마크업을 봅니다.
- 팝업 예상 시간 대조가 첫 번째 것만 보던 것을 고쳤습니다(`popup.js`에 두 군데
  있어서 뒤엣것이 어긋나도 통과했습니다).

### 1.7.6
- **`실행 전에 후보를 다시 확인` 설정을 넣었습니다** (고급, 기본 꺼짐).
  켜면 재차단을 보내기 직전에 후보를 다시 조회해, 그 사이 다른 완장이
  이미 차단한 사람을 빼고 보냅니다. 중복 차단은 기존 차단을 풀고 새로
  걸어서 만료일만 밀리는데 하루 차단 한도는 그대로 씁니다.
- 완장이 한 명뿐이면 겹칠 일이 없어 조회만 늘고 얻는 게 없으므로
  기본은 꺼둡니다.
- **재확인 도중 실패하면 보내지 않고 멈춥니다.** 로그인이 풀렸거나 IP가
  막힌 것일 수 있고, 그 판정은 믿을 수 없습니다. 확인하려고 켠 기능이
  확인에 실패했는데 그대로 보내면 앞뒤가 안 맞습니다.
- 몇 명을 걸러냈는지 기록에 남깁니다.

### 1.7.5
- **명단을 내보낼 때 갤로그 기록도 같이 나갑니다.** 완장이 여럿이면 같은
  사람을 각자 조회하게 됩니다. 한 명이 한 바퀴 돌고 파일로 넘기면 나머지는
  조회 0번으로 기준점을 얻습니다. 부담이 제일 큰 첫 한 바퀴를 통째로
  건너뛰는 것입니다.
- **이미 명단에 있는 사람도 갤로그 기록은 받아옵니다.** 예전에는 코드가
  겹치면 통째로 건너뛰어서, 명단이 같은 완장끼리는 아무것도 넘길 수
  없었습니다.
- 합치는 규칙: 숫자가 같으면 **처음 본 시각은 이른 쪽**, 마지막으로 잰
  시각은 늦은 쪽을 씁니다. 최신으로 덮으면 공유할수록 기준점이 뒤로 밀려서
  아무도 판정을 못 받게 됩니다. 숫자가 다르면 그 사이에 글을 쓴 것이므로
  늦게 잰 쪽을 통째로 씁니다.

### 1.7.4
- **로그인이 풀렸을 때 봐야 할 사람이 0명으로 나오던 것을 고쳤습니다.**
  대상을 고를 때는 `nextCheckAt`이 0인 사람(명단 채우기로 막 담은 사람)을
  '봐야 할 사람'으로 치는데, 세는 쪽만 0을 걸러내고 있었습니다. 한 갤러리
  명단 4575명이 전원 0이라 한 명도 안 세어졌습니다. 이제 대상을 고른
  결과를 그대로 세고, 만료 시각이 지난 사람과 아직 한 번도 못 본 사람을
  갈라서 알려줍니다.
- **예상 시간이 늘 모자라게 나오던 것을 고쳤습니다.** 요청 간격만 세고 조회
  시간을 빼먹어서, 300명에 `6분쯤`이라 해놓고 8~9분이 걸렸습니다. 완장이
  멈춘 줄 알고 기다렸습니다. 갤로그 점검 쪽도 같은 문제였습니다.
- **명단이 크면 며칠에 걸쳐 한 바퀴를 돈다는 것을 알려줍니다.** 4575명이면
  300명씩 16번, 하루 2번이면 8일입니다. 설명이 없어서 고장으로 보였습니다.
- 도는 동안 `지금 확인`이 눌리지 않는 이유도 기록에 남깁니다.

### 1.7.3
- **명단에 넣을 때도 사유를 직접 입력할 수 있습니다.** 지금까지는 드롭다운 7개만
  나와서 `벌레` 같은 사유를 쓸 수 없었습니다. 한 명씩 추가할 때와
  `여러 명 한 번에` 둘 다 `직접 입력` 을 고르면 됩니다. 파일로 불러올 때만
  직접 입력 사유가 살아남던 문제입니다.
- **이미 명단에 있는 사람의 사유와 메모를 고칠 수 있습니다.** 명단 행에
  `수정` 이 생겼습니다. 예전에는 사유를 바꾸려면 지웠다 다시 넣어야 했고,
  그러면 확인 이력이 같이 날아갔습니다.
- **`중지만 보기` 를 넣었습니다.** 중지된 사람은 조회에서 빠지지만 명단에는
  남습니다. 60일간 기록이 없으면 자동으로 중지되기도 합니다. 명단에서 뺄지
  판단하려면 그 사람들만 모아볼 수 있어야 합니다.
- 명단 검색이 **사유로도** 됩니다. 직접 입력 사유를 쓰면 사유가 곧 분류가 됩니다.

### 1.7.2
- **로그인이 풀렸을 때 엉뚱한 안내를 하던 것을 고쳤습니다.** 지금까지는
  `매니저 권한이 있는 갤러리인지 확인하세요` 라고 떠서, 완장이 자기 권한이
  날아간 줄 알고 엉뚱한 곳을 보게 됐습니다. 실제로는 로그인만 다시 하면
  되는 상황입니다.
  디시는 로그아웃 상태에서 안내 문구가 아니라 **87바이트짜리 스크립트 한 줄**을
  돌려줍니다. 본문에 `로그인` 이라는 글자가 없어서 기존 검사가 걸리지
  않았습니다. (2026-09-09 실측)
- 명단 채우기 쪽에도 같은 검사를 넣었습니다. 거기는 로그인 확인이 아예
  없었습니다.

### 1.7.1
- **갤로그 점검이 볼 필요 없는 사람을 건너뜁니다.** 이 점검은 '이 사람이
  N개월간 아무것도 안 했나'를 묻는 것이라, 기준점을 잰 지 N개월이 안 된
  사람은 지금 다시 재도 판정이 안 나옵니다. 중간에 몇 번을 더 재든 판정은
  '처음 잰 값과 지금 값이 같은가'로 똑같습니다. 4533명 명단에 3개월 기준을
  걸면 볼 사람이 1113명으로 줍니다.
- **명단에 담으면서 갤로그 숫자를 미리 기록할 수 있습니다.** 채우기 화면의
  토글입니다. 총 요청 수는 같지만 채우기를 하는 날마다 저절로 나뉩니다.
- **`할 일` 탭에 `밀린 31일 차단 불러오기`를 넣었습니다.** 마지막으로 불러온
  날 이후만 훑고, 끝나면 채우기 화면으로 넘어갑니다. 예전에는 명단 →
  채우기 → 날짜 고르기 → 불러오기로 네 번을 눌러야 했습니다.
- **로그인이 풀렸을 때도 만료 예정 인원을 알려줍니다.** 만료 예정 시각은
  명단에 저장돼 있어 디시에 묻지 않고도 셀 수 있습니다. 예전에는
  `차단 목록을 읽지 못했습니다`만 뜨고 끝나서, 재차단할 사람이 있어도
  모르고 지나갔습니다.
- 채우기 기본 페이지를 10 → 140으로 올렸습니다. 빈 페이지가 나오면 거기서
  멈추므로 작은 갤러리에서 헛돌지 않습니다.
- 갤로그 점검이 '한 번에 N명만 보고 끝난다'는 것을 화면과 기록에 적었습니다.
- 점검 대상을 고르는 규칙을 `dc.js`로 옮겼습니다. `background.js` 안에 두면
  검사가 규칙을 베껴 적게 되고, 그러면 실제 코드가 바뀌어도 통과합니다.

### 1.7.0
- **직접 입력 사유로 차단된 사람을 재차단할 수 있게 됐습니다.** 디시 차단 창은
  라디오 7개(음란성·광고·욕설·도배·혐오 콘텐츠·저작권 침해·명예훼손) 외에
  `직접 입력` 칸이 있고, 완장들은 주로 이쪽을 씁니다. 예전 코드는 이 사유를
  만나면 `알 수 없는 사유`로 던져서 **한 명도 재차단되지 않았습니다.**
  실제 요청을 확인해보니 직접 입력은 `avoid_reason=0` + `avoid_reason_txt=원문`
  으로 갑니다. (여덟 번째 라디오라 8일 거라 짐작했는데 0이었습니다)
- **명단 파일을 불러올 때 사유를 음란성으로 바꾸지 않습니다.** 드롭다운 7개에
  없으면 전부 음란성으로 만들던 것이 원인이었습니다. 한 갤러리 명단 4533명이
  전원 직접 입력 사유(`벌레` 4287명 등)라 통째로 음란성이 됐습니다.
  이제 원문을 그대로 두고, 재차단할 때 그대로 보냅니다.
- 사유가 20자(디시 입력칸 한도)를 넘으면 줄여 보내고 기록에 남깁니다.

### 1.6.9
- DCRefresher Reborn 설정 화면을 실물로 보고 맞췄습니다. 1.6.8에서 남색으로
  바꿨던 건 추측이었고 **틀렸습니다.** 실제로는 중성 짙은 회색이고, 층은
  색상이 아니라 밝기 차로만 가릅니다. 되돌렸습니다.
- 켜진 토글을 초록으로 바꿨습니다. 파랑은 링크에 쓰고 있어 겹쳤습니다.
- 카드에서 테두리를 빼고 모서리를 크게(12px), 여백을 넉넉하게 했습니다.
- 토글 항목을 `설명 왼쪽 · 스위치 오른쪽 끝`으로 정렬했습니다.
- **설정마다 기본값을 적었습니다.** 어떤 값을 건드렸는지 화면에서 보입니다.
  화면에 적힌 숫자가 코드의 기본값과 다르면 검사가 실패합니다.

### 1.6.8
- 색을 어두운 남색 계열로 바꿨습니다. 회색 일변도라 배경·카드·입력칸이 잘
  구분되지 않았습니다.
- **되돌리기 어려운 설정 세 곳을 토글 스위치로 바꿨습니다.** `확인 없이 자동
  재차단`, `알림`, `이미 해제된 사람도 보기`. 체크박스는 켜졌는지 한눈에
  안 들어옵니다.
- 설정 항목을 카드로 묶었습니다. 제목·값·설명이 어디까지 한 항목인지
  보이지 않았습니다.

### 1.6.7
- **탭 6개를 4개로 줄였습니다.** `할 일` / `명단` / `재차단 이력` / `설정`.
  기능이 요청 순서대로 붙는 동안 탭 3개가 전부 '명단'으로 시작하게 됐고,
  탭 순서가 실제 작업 순서(채우기 → 명단 → 후보)의 역순이었습니다.
  명단 관련 세 화면은 `명단` 탭 안에서 `보기 / 채우기 / 빼기`로 고릅니다.
- `명단 정리` → `빼기`. '정리'는 무엇을 하는지 이름에서 안 보였습니다.
- `이력`과 `기록`이 둘 다 있어 헷갈리던 것을 `재차단 이력`과 `작업 기록`으로
  갈랐습니다.
- **`식별자` `식별 코드` `식별코드` 세 표기를 `식별코드`로 통일했습니다.**
- **설정을 기본과 고급으로 갈랐습니다.** IP 차단으로 이어질 수 있는 숫자
  세 개를 접어두고 경고를 붙였습니다. `확인 시각`과 같은 모양으로 나란히
  놓여 있어 위험한 값인지 알 수 없었습니다.
- `빼기` 화면의 갤로그 점검과 활동 점검을 시각적으로 분리했습니다. 서로 다른
  것을 찾는데 옵션 네 개가 한 덩어리로 섞여 있었습니다.
- **명단이 비어 있으면 무엇부터 할지 안내합니다.** 처음 켜면 빈 후보 화면만
  보였습니다.
- **설정의 조회 속도 설명이 `한 명당 0.4초`로 남아 있던 것을 고쳤습니다.**
  1.6.6에서 간격을 1.2초로 올리면서 이 문구를 안 고쳤습니다. 화면 문구와
  실제 간격이 어긋나면 검사가 실패하도록 했습니다.

### 1.6.6
- **정기 확인의 요청 간격을 갤로그 점검과 같은 기준으로 맞췄습니다.**
  1.6.5에서 갤로그만 1.2초 + 난수로 고치고, 정기 확인은 0.4초 고정인 채로
  남아 있었습니다. 정기 확인도 한 명당 요청 한 번이라 IP 차단 사고 때와
  모양이 같습니다. 게다가 이쪽은 사람이 누르지 않아도 하루 두 번 저절로
  돌고, 명단 채우기로 새로 담은 사람은 전원이 한꺼번에 조회 대상이 되므로
  기본 상한(300명)까지 연달아 요청이 나갔습니다.
- **조회 예상 시간이 3배 부풀려 나오던 것을 고쳤습니다.** 간격은 0.4초인데
  1.2초로 계산하고 있었습니다. 이제 실제 간격을 보고 계산합니다.
- 검사에 `background.js에 고정 간격 대기가 없다`를 넣었습니다. 상수만
  검사하면 코드가 다시 숫자를 직접 쓰는 쪽으로 돌아가도 통과합니다.
  1.6.5가 정확히 그 상태였습니다.

### 1.6.5
- **하루 차단 한도에 걸려 한 명도 못 보냈을 때 성공처럼 보이던 문구를
  고쳤습니다.** `0건 모두 31일 차단 확인됨` / `완료: 0/0건 확인` 이라고 나왔습니다.
  이제 `한 명도 보내지 못했습니다` / `재차단된 사람이 없습니다. 후보 N명은 그대로
  남아 있습니다` 로 나옵니다. (2026-09-08 실제 실행에서 발견)
- **갤로그 글·댓글 수를 못 읽던 문제를 고쳤습니다.** 숫자가 1,000을 넘으면
  `(4,972)` 처럼 쉼표가 붙는데, 그걸 못 읽고 넘어갔습니다. 활동이 많은 사람일수록
  걸리는 문제라 큰 갤러리에서는 대부분이 여기 해당했습니다. 활동이 적은 계정은
  쉼표가 없어서 겉으로는 멀쩡해 보였습니다. (2026-09-08 실제 갤로그로 확인)
- **갤로그 점검이 IP 차단을 부르지 않도록 고쳤습니다.** 2026-09-08에 실제로
  차단당한 사례가 있었습니다. 300명을 0.4초 간격으로 연달아 조회한 직후였습니다.
  - 요청 간격을 1.2초로 늘리고 **매번 조금씩 다르게** 흔듭니다. 일정한 간격
    자체가 사람이 만들 수 없는 신호라 그렇습니다.
  - 한 번에 확인하는 인원 기본값을 300명에서 **50명**으로 줄였습니다. 오래
    확인 안 한 사람부터 도니, 며칠에 나눠 눌러도 결국 전원을 다 봅니다.
  - **연속으로 못 읽으면 그 자리에서 멈춥니다.** 예전에는 이미 막힌 뒤에도
    남은 사람 수만큼 계속 요청을 보냈습니다.
  - 막혀서 못 읽은 것인지, 갤로그 화면이 바뀌어서 못 읽은 것인지 **구분해서**
    알려줍니다. 예전에는 둘 다 "화면 구조가 바뀐 것 같습니다"라고만 했습니다.
  - 12시간 안에 확인한 사람은 건너뜁니다. 실수로 두 번 눌러도 부담이 없습니다.
- **명단 채우기가 지금 차단 중인 사람만 보여줍니다.** 이미 해제된 사람이 섞여
  나오던 것을 고쳤습니다. 해제된 사람도 봐야 하면 `이미 해제된 사람도 보기`를
  켜세요. 몇 명을 걸러냈는지는 기록에 남습니다.
- **명단 채우기를 다시 불러오면 체크 상태가 초기화됩니다.** 지난번에 손으로
  풀어둔 체크가 새 결과에 그대로 남아, 어떤 사람은 체크가 풀린 채로 보이던
  문제입니다.
- **파일에 사유가 없으면 알려줍니다.** 옛 버전으로 내보낸 명단 파일은 사유 칸이
  없어서 전원 '음란성'으로 들어갔는데, 아무 안내가 없었습니다.
- 명단 정리 탭에서 갤로그 점검을 위로 올렸습니다. 기간 선택지에 2개월을
  넣었습니다.
- **'완장이 직접 해제' 목록이 사라지던 문제를 고쳤습니다.** 이 목록은 7일 뒤에
  다시 확인하도록 되어 있는데, 그 사이에 `지금 확인`을 한 번 누르면 목록과
  `명단에서 빼기` 버튼이 통째로 없어졌습니다. 하루 차단 한도에 걸려 남겨둔
  후보도 같은 이유로 사라졌습니다. 이제 이번에 조회하지 않은 사람의 판정은
  그대로 둡니다.
- **화면 구조가 바뀌었을 때 조용히 넘어가던 문제를 고쳤습니다.** 차단/해제
  칸이나 닉네임 칸을 못 읽으면 행 수는 딱 맞아떨어져서 기존 경고에 걸리지
  않았습니다. 그러면 해제된 사람이 전원 '차단 중'으로 보이고 후보가 0건이
  되는데, 화면에는 `이상 없음` 이라고만 떴습니다. 이제 알림이 뜨고, 그 상태에서는
  자동 실행도 건너뜁니다.
- 차단 목록·글 목록을 훑을 때도 요청 간격을 흔듭니다.
- 갤로그 점검 예상 시간이 실제와 맞습니다. 명단이 적으면 `3초쯤 걸립니다`
  처럼 초로 알려주고, 확인창도 입력한 상한이 아니라 실제로 확인할 인원으로
  셉니다. 명단 2명에 `1분쯤 걸립니다` 라고 하던 문제입니다.

### 1.6.4
- **탈퇴한 계정을 '코드 확인 필요'로 잘못 표시하던 문제 수정.** 갤로그가 삭제되면
  브라우저에서는 안내 화면이 뜨지만, 확장이 실제로 받는 응답은 주소를 바꾸라는
  스크립트 한 줄뿐입니다. 그 안의 경로를 보고 판단하도록 고쳤습니다.
- 탈퇴한 계정과 존재하지 않는 코드는 여전히 구분됩니다. 식별 코드를 잘못 읽었을 때
  멀쩡한 사람이 탈퇴로 찍히는 걸 막는 장치라 그대로 둡니다.

### 1.6.2
- 하루 차단 한도 문구를 실제로 확인해 반영했습니다. 한도에 걸리면 1시간·6시간
  차단은 여전히 된다는 것도 안내합니다.
- **갤로그 점검이 글·댓글 수도 같이 기록합니다.** 다음 점검 때와 비교해 숫자가
  그대로면 그동안 디시에서 아무것도 안 한 것입니다. 지금까지의 활동 점검은
  해당 갤에 쓴 글만 봐서 댓글로만 노는 사람을 못 잡았는데, 그 구멍이 막힙니다.
  요청은 늘지 않습니다. 이미 열고 있던 페이지에서 숫자만 더 읽습니다.
- 비공개 갤로그도 개수는 보이므로 공개 여부와 무관하게 동작합니다.
- **첫 점검에서는 아무것도 나오지 않습니다.** 비교할 이전 숫자가 없기 때문입니다.
  한 번 더 돌려야 뜻이 생깁니다.

### 1.6.1
- **명단 채우기에서 날짜를 넣으면 날짜가 기준이 됩니다.** 전에는 페이지
  수(기본 10)가 먼저 걸려서, 날짜를 넣어도 10페이지에서 멈췄습니다.
- **명단 내보내기에 메모와 사용 여부가 들어갑니다.** 확장을 다시 깔아도
  메모가 그대로 살아납니다. 남에게 넘기는 후보 파일은 전처럼 코드와 사유만
  담습니다.
- 파일에 모르는 사유가 있으면 조용히 바꾸지 않고 몇 명인지 알려줍니다.
- 명단 정리 표의 머리글 체크박스가 동작합니다. 넣어만 두고 기능을 붙이지
  않아서 눌러도 아무 일이 없었습니다.

### 1.6.0
- **명단 정리 탭을 추가했습니다.** 명단에서 뺄 만한 사람을 찾습니다.
  자동으로 지우지 않고, 확인 후 직접 지웁니다.
- **활동 점검** — 갤 글 목록을 훑어 최근에 글을 쓴 식별 코드를 모읍니다.
  글 목록의 글쓴이 칸에 식별 코드가 들어 있어서, 명단이 몇 명이든
  요청 수가 같습니다. 3개월 기준으로 하루 100글 갤러리면 94페이지입니다.
- **갤로그 점검** — 탈퇴한 계정을 찾고, 글·댓글 수를 기록합니다. 한 명당 요청 한 번이라 느립니다.
  활동 점검으로 명단을 한 번 걸러낸 뒤에 돌리는 게 좋습니다.
- 갤로그를 보기 위해 `gallog.dcinside.com` 권한이 추가됐습니다.

### 1.5.8
- **명단 채우기에 `이 날짜까지` 를 넣었습니다.** 페이지 수를 감으로 찍지 않아도
  됩니다. 그 날짜보다 오래된 차단이 나오면 거기서 멈춥니다.
- **페이지 상한을 3000으로 올렸습니다.** 최대 90,000행입니다.
  1000페이지가 넘으면 요청 간격을 500ms로 넓힙니다.
- 수천 페이지를 훑어도 메모리가 늘지 않게 고쳤습니다. 예전에는 본 행을 전부
  기억했는데, 직전 페이지만 있으면 충분합니다.

### 1.5.7
- **명단 채우기 페이지 상한을 100 → 500으로 올렸습니다.** 한 페이지가 30행이니
  최대 15,000행까지 훑습니다. 수백 페이지는 몇 분 걸리며, 도는 동안 다시 누르면
  막습니다.
- **하루 차단 한도를 알아챕니다.** 디시는 완장 계정마다 하루 차단 횟수를
  제한합니다. 한도에 걸리면 남은 요청을 보내지 않고 멈추며, 못 보낸 사람은
  후보 목록에 그대로 남겨 다음에 이어서 할 수 있습니다.
  보내지도 않은 사람을 '실패'로 기록하지 않습니다.
- **후보 탭에 `남은 후보 복사`를 넣었습니다.** 사유별로 정리된 코드 목록이
  복사됩니다. 갤러리에 그대로 올리면 다른 완장이 이어서 차단할 수 있습니다.
  닉네임이나 메모는 넣지 않습니다.
- **명단 탭에 `여러 명 한 번에`를 넣었습니다.** 갤러리에 올라온 글을 통째로
  붙여넣어도 코드만 골라냅니다.

### 1.5.6
- 동작에는 변화가 없습니다. 코드 정리와 테스트 보강입니다.
- `isBusy` / `localDateKey` / 검증 페이지 수 계산이 `dc.js`와 `background.js`에
  따로 적혀 있던 것을 한 곳으로 모았습니다. 양쪽에 같은 식이 있으면 한쪽만
  고쳤을 때 테스트가 눈치채지 못합니다.
- `test.mjs`를 실제 관리 화면 마크업으로 다시 썼습니다. 예전 픽스처는 지어낸
  구조라 통과해도 실제 동작을 보장하지 못했습니다. 페이지 순회 검사도 넣었습니다.

### 1.5.5
- **명단 채우기에 사유 필터를 추가했습니다.** 불러온 뒤 사유 칩을 눌러
  그 사유로 차단했던 사람만 추릴 수 있습니다. 사유별 인원이 함께 표시되고,
  `보이는 사람 전체 선택`은 걸러낸 사람에게만 적용됩니다.
  (파딱 요청 반영)
- 명단에 담기 전 확인창에 사유별 인원을 보여줍니다.
- 아무도 선택되지 않으면 `명단에 추가` 버튼이 눌리지 않습니다.

### 1.5.4
- **명단 채우기가 1페이지만 불러오던 문제 수정.** 페이지 넘김을 화면의 페이저
  링크에서 읽어오다가, 페이지 위쪽 즐겨찾기 레이어의 페이저를 먼저 잡아
  링크를 하나도 못 찾고 있었습니다. 이제 링크를 보지 않고 페이지 번호를
  직접 올리며 읽고, 끝은 내용으로 판단합니다.
- 페이지 넘김이 실제로 됐는지 기록 창에 남깁니다.
  (`N페이지에서 차단 이력 M행을 읽었습니다`)
- 재차단 검증 시 훑을 페이지 수를 실제 페이지 크기(30행)에 맞춰 계산합니다.

### 1.5.3 (내부 수정, 릴리즈 없음)
- 표에 행이 있는데 읽지 못하면 경고와 알림을 띄웁니다. 예전에는 빈 결과가
  '이력 없음'과 구분되지 않아, 아무도 재차단되지 않는데도 조용히 넘어갔습니다.
- 차단/해제 상태를 읽지 못한 행도 파싱 실패로 셉니다.
- 사유 표기의 공백 차이를 흡수합니다. (`혐오콘텐츠` → `혐오 콘텐츠`)
- 긴 조회 도중 작업 표시가 만료돼 실행이 겹치던 문제 수정.

### 1.5.2
- 서비스워커가 작업 도중 종료되면 '작업 중' 표시가 남아 확장이 조용히
  멈추던 문제 수정. 30분이 지난 표시는 무시하고, 브라우저 재시작 시 풉니다.
- 설정을 저장할 때 화면에 없던 항목이 지워지던 문제 수정.
  조회 인원과 점검 인원을 설정 탭에서 조절할 수 있습니다.
- 디시가 표 마크업에 속성을 덧붙여도 견디도록 파싱을 넓혔습니다.
- 재차단 검증에서 못 찾은 대상을 상한 없이 전부 개별 확인합니다.
  예전에는 20건을 넘으면 확인 없이 실패로 기록했습니다.

## 명단 정리

명단이 커지면 이미 떠난 사람이 계속 남습니다. 두 가지로 걸러냅니다.

**활동 점검**은 갤 글 목록을 훑습니다. 글 목록의 글쓴이 칸에 식별 코드가
들어 있어서, 한 번 훑으면 명단 전원의 활동 여부를 동시에 알 수 있습니다.
사람마다 조회하는 방식이 아니라 명단이 커져도 요청이 늘지 않습니다.

**갤로그 점검**은 탈퇴한 계정을 찾습니다. 갤로그 주소가
`gallog.dcinside.com/_error/deleted` 로 넘어가면 탈퇴입니다.
한 명당 요청 한 번이라 느리니, 활동 점검을 먼저 돌려 대상을 줄이세요.

결과는 근거가 확실한 순서로 보여줍니다.

| 표시 | 뜻 | 근거 |
|---|---|---|
| 탈퇴 | 갤로그가 삭제됨 | 확실 |
| 코드 확인 필요 | 갤로그가 404 | 코드를 잘못 읽었을 수 있음. 지우지 말고 확인 |
| 글 없음 | 목록에 안 나옴 | 약함. 댓글만 다는 사람도 여기 걸림 |

**어느 쪽도 자동으로 지우지 않습니다.** 글 목록에는 댓글이 나오지 않아서,
댓글로만 활동하는 사람이 '글 없음'으로 잡힙니다. 지우기 전 확인창에서
그런 사람이 몇 명 섞여 있는지 알려줍니다.

비공개 갤로그와 디시가 차단한 갤로그는 '정상'으로 분류돼 명단에 남습니다.
못 알아본 것을 지우는 것보다 남기는 편이 안전합니다.

## 완장끼리 후보 나누기

디시는 완장 계정마다 하루 차단 횟수를 제한합니다. 매니저도 걸립니다.
후보가 그보다 많으면 한도에 걸린 시점에서 멈추고, 못 보낸 사람은
**후보 목록에 그대로 남습니다.** 시도조차 안 한 사람을 실패로 적지 않습니다.

한도에 걸려도 1시간·6시간 차단은 됩니다. 막히는 건 하루 이상짜리뿐입니다.
당장 막아야 할 사람이 있으면 관리 화면에서 짧게 걸어두고 내일 다시 실행하세요.

후보 탭의 `남은 후보 복사`를 누르면 이런 형태로 복사됩니다.

```
# 재차단 후보 3명 (2026-09-07)

# 음란성 2명
aaa0001
aaa0002

# 광고 1명
bbb0001
```

이걸 갤러리에 올리면 다른 완장이 이어서 처리할 수 있습니다. 받는 쪽은
디시 직접 차단 창에 붙여넣어도 되고, 명단 탭의 `여러 명 한 번에`에
글 전체를 그대로 붙여넣어도 됩니다. 코드만 골라냅니다.

### 파일로 주고받기

용도가 둘이라 담기는 내용이 다릅니다.

**후보 내보내기** (후보 탭) — 남에게 넘기는 용도입니다. **식별 코드와 사유만**
들어갑니다. 닉네임과 메모는 넣지 않습니다. 갤러리에 올려도 됩니다.

**명단 내보내기** (명단 탭) — 내 백업 용도입니다. 사유와 **메모, 사용 여부까지**
같이 들어갑니다. 브라우저를 옮기거나 확장을 다시 깔았을 때 그대로 되살아납니다.
메모가 들어 있으니 공개된 곳에는 올리지 마세요. 내보낼 때 한 번 물어봅니다.

받는 쪽은 명단 탭 → `여러 명 한 번에` → `파일 불러오기` 입니다.
다른 갤러리에서 내보낸 파일이면 가져오기 전에 알려줍니다.
이 확장이 만든 파일이 아니어도, 코드 목록이 든 텍스트 파일이면 코드만 골라냅니다.

파일에 우리가 모르는 사유가 들어 있으면 `음란성` 으로 넣되, 몇 명이 그렇게
됐는지 가져오기 전에 알려줍니다.

디시 게시글에는 파일을 첨부할 수 없습니다. 갤러리에 공개로 올릴 때는
`남은 후보 복사` 를, 완장끼리 직접 주고받을 때는 파일을 쓰세요.

차단 목록을 비공개로 두는 갤러리라면 코드를 공개 게시물에 올리는 것이
적절한지 완장단에서 먼저 정하세요.
